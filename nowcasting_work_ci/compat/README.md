# compat

Legacy CLI entrypoints live here.

Rules:

- New users should prefer top-level [`run.py`](/C:/Users/hp/Desktop/mlblack/nowcasting_work_ci/run.py).
- Historical script names stay available through thin top-level shims.
- Real compatibility implementations belong under `compat/`, not in package root.

