# Start Here: symbolic_orthogonal_nested

```powershell
python examples/cases/symbolic_orthogonal_nested/run_project.py --check
python examples/cases/symbolic_orthogonal_nested/run_project.py --check --build-check
```

Use `cases/<case>/run_solver.py` only for single-Case debugging. Formal runs start at `run_project.py`.
