# Start Here: cross_framework

```powershell
python examples/cases/cross_framework/run_project.py --check
python examples/cases/cross_framework/run_project.py --check --build-check
```

Use `cases/<case>/run_solver.py` only for single-Case debugging. Formal runs start at `run_project.py`.
