# Start Here: matrix_factorization

```powershell
python examples/cases/matrix_factorization/run_project.py --check
python examples/cases/matrix_factorization/run_project.py --check --build-check
```

Use `cases/<case>/run_solver.py` only for single-Case debugging. Formal runs start at `run_project.py`.
