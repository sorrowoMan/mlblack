# Start Here: tiny_transformer_smoke

```powershell
python examples/cases/tiny_transformer_smoke/run_project.py --check
python examples/cases/tiny_transformer_smoke/run_project.py --check --build-check
```

Use `cases/<case>/run_solver.py` only for single-Case debugging. Formal runs start at `run_project.py`.
