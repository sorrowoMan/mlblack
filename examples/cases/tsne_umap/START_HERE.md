# Start Here: tsne_umap

```powershell
python examples/cases/tsne_umap/run_project.py --check
python examples/cases/tsne_umap/run_project.py --check --build-check
```

Use `cases/<case>/run_solver.py` only for single-Case debugging. Formal runs start at `run_project.py`.
