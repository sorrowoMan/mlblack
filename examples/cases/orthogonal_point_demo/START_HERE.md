# Start Here: orthogonal_point_demo

```powershell
python examples/cases/orthogonal_point_demo/run_project.py --check
python examples/cases/orthogonal_point_demo/run_project.py --check --build-check
```

Use `cases/<case>/run_solver.py` only for single-Case debugging. Formal runs start at `run_project.py`.
