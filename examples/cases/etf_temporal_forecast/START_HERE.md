# Start Here: etf_temporal_forecast

```powershell
python examples/cases/etf_temporal_forecast/run_project.py --check
python examples/cases/etf_temporal_forecast/run_project.py --check --build-check
```

Use `cases/<case>/run_solver.py` only for single-Case debugging. Formal runs start at `run_project.py`.
