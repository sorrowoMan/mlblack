# Start Here: traffic_congestion

```powershell
python examples/cases/traffic_congestion/run_project.py --check
python examples/cases/traffic_congestion/run_project.py --check --build-check
python examples/cases/traffic_congestion/run_project.py --group symbolic --check --build-check
```

Use `cases/<case>/run_solver.py` only for single-Case debugging. Formal runs start at `run_project.py`.
