# Start Here: mall_ai_assistant

```powershell
python examples/cases/mall_ai_assistant/run_project.py --check
python examples/cases/mall_ai_assistant/run_project.py --check --build-check
```

Use `cases/<case>/run_solver.py` only for single-Case debugging. Formal runs start at `run_project.py`.
