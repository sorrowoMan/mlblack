# 机器学习术语全景：从哪里来，到哪里去

## 阅读指南

这不是词典。这是一条**因果链**——每个术语的出现都是因为前一个东西不够用了。

整条链只有一个主线：**数据 → 模型 → 预测**，所有术语都在某条瓶颈上被迫出现了解决方案。

**看这篇文章的方法**：顺着读。每个词都跟在它前面的词有因果关系。如果跳到后面看不懂，回到它前面的段落。

---

## 第〇层：问题本身——什么是"学习"

### 0.1 人类怎么学习？

看 10 个苹果 → 归纳"苹果"的特征（红/圆/有梗）→ 见到第 11 个东西判断是不是苹果。

机器的"学习"完全一样：

```
训练数据（10个苹果的图片+标签）
  → 归纳规律（调整模型内部参数）
    → 对新数据做判断（第11张图：是苹果吗？）
```

> 术语：**训练 training / 学习 learning** — 这整个"看数据→调参数"的过程
> 术语：**推理 inference / 预测 prediction** — 训练完后，对新数据做判断

### 0.2 为什么机器能学？

关键假设：**训练数据和新数据来自同一个分布**。

如果训练数据全是红苹果，但现实中有绿苹果，机器学不到。这叫——

> 术语：**分布偏移 distribution shift** — 训练和实际数据分布不同
> 术语：**独立同分布 i.i.d.** — 假设每条数据相互独立且来自同分布（最重要的假设）

### 0.3 一个贯穿全程的例子

为了有直觉，全文中请记住这个具体问题：

| | |
|---|---|
| **任务** | 预测房价 |
| **输入 x** | 面积(100m²)、卧室数(3)、楼层(5)、地段编码(2) |
| **输出 y** | 价格 500万 |

机器学习就是在说：给我 1000 套房的面积/卧室/楼层/地段 → 价格，让我学会一个函数 f，使得：

`f(面积, 卧室, 楼层, 地段) ≈ 真实价格`

然后对第 1001 套房也能猜准。

---

## 第一层：最基本的设定

### 1.1 函数拟合（最古老的直觉）

人类观察世界的方式：输入 x → ??? → 输出 y

我们不知道中间的 ???，所以叫它**函数** f(x)。

目标：找一个 f，使得 f(x) ≈ y 对所有见过的数据成立，并且对没见过的数据也尽量成立。

这就是**所有**机器学习——没有例外。

> 术语：**回归 regression** — 预测连续数值（房价 500 万）
> 术语：**分类 classification** — 预测离散类别（苹果/香蕉/橘子）

**为什么会出现这两个词？** 因为 y 的类型不同：连续 vs 离散。仅此而已。底层都是函数拟合。

---

### 1.2 损失函数——怎么知道 f 好不好

需要一个数字来衡量 f(x) 和 y 差多远。

> 术语：**损失函数 loss function** / **代价函数 cost function** — 衡量预测与真实的差距

#### MSE（均方误差）

```
MSE = (1/n) * Σ(y_true - y_pred)²
```

| 真实价 | 预测价 | 误差 | 平方 |
|--------|--------|------|------|
| 500 万 | 480 万 | 20 | 400 |
| 300 万 | 330 万 | -30 | 900 |
| 800 万 | 750 万 | 50 | 2500 |
| **MSE** | | | **(400+900+2500)/3 = 1267** |

**特点**：大误差被平方放大，所以会**惩罚离群点**。如果你的数据有异常值（比如一套房标价1亿），MSE 会拼命去拟合这个异常值，可能把整个模型带偏。

#### MAE（平均绝对误差）

```
MAE = (1/n) * Σ|y_true - y_pred|
```

同上例子：MAE = (20+30+50)/3 = 33.3

**特点**：不平方，对所有误差一视同仁。对异常值更**鲁棒**（robust —— 不敏感）。

#### 什么时候用哪个？

| | 用 MSE | 用 MAE |
|---|---|---|
| 数据干净 | ✓ | |
| 有异常值 | | ✓ |
| 梯度下降优化 | ✓（可导，好算） | 次之（0 点不可导） |

#### 交叉熵 Cross-Entropy（分类用）

分类的输出不再是数字，而是"属于各类的概率"。

```
预测 [0.7, 0.2, 0.1]   → 70% 概率是苹果
真实 [1.0, 0.0, 0.0]   → 100% 是苹果

交叉熵 = -[1.0*log(0.7) + 0.0*log(0.2) + 0.0*log(0.1)]
       = -log(0.7)
       = 0.357
```

如果预测很自信（预测苹果概率 0.99），交叉熵 = -log(0.99) = 0.01（很小，好）。
如果预测错了（预测苹果概率 0.01），交叉熵 = -log(0.01) = 4.6（很大，差）。

**直觉**：交叉熵衡量"你的预测概率分布和真实分布差多少"。完全一样 = 0，完全不一样 = 很大。

#### Huber Loss（MSE 和 MAE 的折中）

```
误差 < δ 时：用 MSE（更平滑）
误差 ≥ δ 时：用 MAE（对异常值鲁棒）
```

> 术语：**Huber Loss** — 近处平方、远处线性
> 术语：**鲁棒性 robustness** — 模型对异常值/噪声不敏感的程度

---

### 1.3 优化——怎么找到最好的 f

有了损失函数，问题变成：**找到使损失最小的参数**。

> 术语：**参数 parameter** — f 里面可调整的数值（权重 w，偏置 b）
> 术语：**超参数 hyperparameter** — 控制学习过程的设置（学习率、层数），不是从数据学的
> 术语：**优化 optimization** — 找最优参数的过程

#### 用房价例子理解参数和超参数

```python
# 假设 f(x) = w₁*面积 + w₂*卧室数 + b

w₁ = 3.5    # ← 参数：从数据学的，"每平米值 3.5 万"
w₂ = 50     # ← 参数：从数据学的，"多个卧室贵 50 万"
b  = -100   # ← 参数：从数据学的，基础偏移

学习率 = 0.01  # ← 超参数：人定的，"每步走多远"
```

参数是机器自己找的，超参数是你（或自动搜索）定的。

这就是你的 nsgablack/mlblack 做的事——只不过它们不局限于梯度，还可以用进化、模拟退火、VNS 等任何搜索策略。

---

### 1.4 训练集/验证集/测试集——怎么知道 f 真的"学会了"

你不能用考试题来复习。

> 术语：**训练集 training set** — 用来调参数的题（做作业）
> 术语：**验证集 validation set** — 用来调超参数、选模型的题（模拟考）
> 术语：**测试集 test set** — 只用于最终评价、绝不允许偷看的题（高考）

```
全部数据 10000 条
  ├── 训练集 7000 条 ← 模型看这些，调 w, b
  ├── 验证集 1500 条 ← 模型不看，但"你"看：调学习率、选层数
  └── 测试集 1500 条 ← 谁都不能看！只在最后提交论文/上线时用
```

> 术语：**数据泄露 data leakage** — 测试集信息意外进入了训练（最严重的错误之一）
> 术语：**交叉验证 cross-validation** — 把数据分成 K 份，轮流用 1 份验证、K-1 份训练，提高可靠性

```
K-Fold Cross Validation (K=5)：

折1: [训训训训] [验]  → 得分 0.85
折2: [训训训验] [训]  → 得分 0.82
折3: [训训验训] [训]  → 得分 0.88
折4: [训验训训] [训]  → 得分 0.84
折5: [验训训训] [训]  → 得分 0.86
              平均 = 0.85  ← 比单次划分更可靠
```

> 术语：**stratified K-fold** — 保持每折中各类别比例与原始数据一致（分类专用）

---

## 第二层：线性模型——最简单的 f

### 2.1 线性回归

```python
f(x) = w₁*x₁ + w₂*x₂ + ... + wₙ*xₙ + b
```

房价例子：`价格 = 3.5 * 面积 + 50 * 卧室数 - 100`

> 术语：**系数 coefficient / 权重 weight** — w₁, w₂, ...，每个特征有多重要
> 术语：**偏置 bias / 截距 intercept** — b，当所有特征都是 0 时的基础值
> 术语：**残差 residual** — 预测值和真实值的差 `y_true - y_pred`

#### 怎么求 w 和 b？

##### 方法一：解析解（Normal Equation）

```
w = (XᵀX)⁻¹ Xᵀ y
```

直接算出来，不需要迭代。但 X 很大时矩阵求逆 O(n³) 太慢。

##### 方法二：梯度下降（迭代搜索）

```
w_new = w_old - 学习率 * ∂Loss/∂w
```

反复沿最陡下降方向走一小步，直到收敛。需要成千上万个数据点时，梯度下降反而比解析解快。

> 术语：**闭式解 closed-form solution** — 解析解，一步算出来的精确解
> 术语：**迭代方法 iterative method** — 逐步逼近最优解

---

### 2.2 逻辑回归（名字骗了你——它是分类模型）

为什么叫"回归"却做分类？因为它里面还是线性回归，外面包了一层 sigmoid 把输出压到 [0,1]：

```python
z = w₁*x₁ + w₂*x₂ + b      # ← 线性回归（回归部分）
p = 1 / (1 + e^(-z))        # ← sigmoid 压到 0~1（概率）
if p > 0.5: 预测 = 苹果
else:       预测 = 不是苹果
```

> 术语：**sigmoid** — 把任意实数映射到 (0,1) 的 S 形函数
> 术语：**决策边界 decision boundary** — 分界超平面，w·x + b = 0
> 术语：**odds / log-odds** — 几率和对数几率，逻辑回归的数学基础

**为什么逻辑回归还活着？** 因为：
1. 可解释：`w₁` 就是"面积每多 1m²，odds 乘 e^w₁"
2. 概率校准好：输出的概率值很准
3. 作为神经网络的最后一层依然在用

> 术语：**softmax** — 逻辑回归的多类版，输出各类概率，和为 1
> 术语：**sigmoid vs softmax** — 二分类用 sigmoid，多分类用 softmax

---

### 2.3 线性模型的致命局限

```python
f(x) = w₁*x₁ + w₂*x₂ + b
```

这个函数只能表示**平面**。现实世界的关系不是平面。

```
现实：价格 = 3*面积 + 50*卧室 + 0.2*面积*卧室（有交互项）
线性：价格 = w₁*面积 + w₂*卧室         （只会画平面）
```

**后果**：线性模型对非线性关系无能为力。

两条出路：
- 出路 A：把 x 变换到高维空间，让线性分隔够用 → 核方法
- 出路 B：人手构造非线性特征（x₁²、x₁*x₂、log x₁） → 特征工程
- 出路 C：换一个能学非线性的模型 → 神经网络/树模型

---

### 2.4 核方法——不改模型，改空间

> 术语：**核方法 kernel method** — 把数据映射到高维空间，在高维做线性分类

```
原始 2D：● ○ ●      线性分不开
         ○ ● ○

升维：  计算 x²、y²、xy、...
高维：  ● ● ●       线性分得开！
         ○ ○ ○
```

> 术语：**核函数 kernel** — 隐式计算高维内积 `K(x₁, x₂) = φ(x₁)·φ(x₂)`
> 术语：**核技巧 kernel trick** — 不用真的算出 φ(x)，只算内积，省计算
> 术语：**SVM（支持向量机）** — 找最大间隔超平面 + 可用核函数
> 术语：**RBF 核 / 高斯核** — 最常用的核，相当于无限维映射
> 术语：**支持向量 support vectors** — 离决策边界最近的训练点，决定了边界位置
> 术语：**软间隔 soft margin** — 允许一些数据点在边界内或错误一侧（C 参数控制）

```
SVM 的直觉：
  ┃
  ┃  ○  ○     SVM 不只"分开了就行"
  ┃    ┃      而是"让分界线离最近的点尽可能远"
  ┃  ●┃●      → 泛化更好
  ┃    ┃
  ┃  ○  ○
  ┃
  ← 间隔 margin →
```

**为什么核方法后来被深度学习取代？** 核函数是手工设计的（选 RBF？选多项式？），深度学习自动学映射。但小数据 + 非线性时，SVM 仍然很好。

---

### 2.5 特征工程——人帮模型变聪明

> 术语：**特征工程 feature engineering** — 用人类知识变换输入，使线性模型够用
> 术语：**特征选择 feature selection** — 从大量特征中选有用的

```python
# 原始数据只有 3 个特征
面积, 卧室数, 房龄

# 特征工程后变成 6 个
面积, 卧室数, 房龄,  面积*卧室数,  面积²,  log(房龄+1)
```

> 术语：**交互项 interaction term** — x₁ * x₂，捕捉特征间的共同效应
> 术语：**多项式特征 polynomial features** — x², x³，让线性模型能拟合曲线
> 术语：**独热编码 one-hot encoding** — 把"地段=A/B/C"变成 [1,0,0], [0,1,0], [0,0,1]
> 术语：**标签编码 label encoding** — 把"A/B/C"变成 0/1/2（注意：隐含了顺序！）
> 术语：**标准化 standardization / Z-score** — `(x - mean) / std`，使特征均值0方差1
> 术语：**归一化 normalization / MinMax** — 缩放到 [0,1] 或 [-1,1]
> 术语：**分箱 binning / discretization** — 把连续值切成区间（年龄→青年/中年/老年）
> 术语：**目标编码 target encoding** — 用该类别对应的 y 均值替代类别标签

#### 标准化 vs 归一化 什么时候用？

```
标准化：(x - x̄) / σ    → 假设数据近似正态分布
归一化：(x - min) / (max - min) → 有明确上下界时

梯度下降 → 用标准化（稳定梯度）
距离度量 → 用标准化或归一化（防止大尺度特征主导）
树模型   → 不需要！（树的划分只看相对顺序）
```

---

## 第三层：决策树家族——不拟合函数，做判断

### 3.1 决策树

不找 `f(x₁, x₂)`，而是做一系列 if-else：

```
面积 > 100m²？
  ├── 是 → 卧室数 > 2？
  │         ├── 是 → 预测 800万
  │         └── 否 → 预测 500万
  └── 否 → 房龄 > 20年？
            ├── 是 → 预测 200万
            └── 否 → 预测 350万
```

> 术语：**决策树 decision tree** — 递归分割特征空间
> 术语：**根节点 root node** — 第一个判断（面积>100?）
> 术语：**内部节点 internal node** — 中间判断
> 术语：**叶节点 leaf node** — 最终预测
> 术语：**信息增益 information gain** — 用某个特征分割后，不确定性减少了多少
> 术语：**基尼系数 Gini impurity** — "随机抽两个样本，标签不同的概率"，越低越纯
> 术语：**熵 entropy** — 不确定性的度量，`-Σ p*log(p)`
> 术语：**剪枝 pruning** — 切掉过深的分支防止过拟合

#### 信息增益怎么算？

```
分割前：100套房里 50套>500万, 50套≤500万 → 很混乱，Gini=0.5
按"面积>100?": 左边 70套(45套贵), 右边 30套(5套贵) → 各边较纯
信息增益 = 0.5 - (70/100*0.49 + 30/100*0.28) = 显著降低 → 好分割
```

> 术语：**CART** — 最常用的决策树算法（二叉树，Gini/分类，MSE/回归）
> 术语：**ID3 / C4.5** — 早期的决策树算法（多叉树，信息增益/信息增益率）

---

### 3.2 单棵树的局限

1. **过拟合**：树太深 = 记完了训练数据 = 泛化差
2. **不稳定**：数据变一点点，树结构可能剧变
3. **高方差**：不同训练集长出来的树差别很大

---

### 3.3 集成方法——多棵树投票

> 术语：**集成学习 ensemble** — 多个模型组合，效果通常 > 单个最好模型

两条根本不同的路线：

```
Bagging（并行）                 Boosting（串行）
├── 每棵树独立训练              ├── 每棵树依赖前一棵
├── 用 bootstrap 采样数据       ├── 调整样本权重
├── 降方差（稳定）              ├── 降偏差（准）
└── 代表：随机森林              └── 代表：XGBoost
```

#### Bagging 为什么有效？

```
单棵树预测：500万（偏了）
100棵树平均：480万, 510万, 495万, ...
          → 平均 490万（偏差互相抵消）
```

> 术语：**Bootstrap** — 有放回采样，生成不同训练子集
> 术语：**Bagging** = Bootstrap Aggregating — 并行训练 + 取平均
> 术语：**OOB（Out-of-Bag）** — bootstrap 没选中的样本，可当免费验证集

#### 随机森林 = Bagging + 随机特征选择

```
随机森林的"两次随机"：
1. bootstrap 随机抽取训练样本     → 每棵树看到的人不同
2. 每次分裂随机选部分特征          → 每棵树关注的角度不同

→ 双重随机 = 树之间差异大 = 集成效果好
```

> 术语：**随机森林 Random Forest** — 上述方案
> 术语：**max_features** — 每次分裂随机选的特征数（`sqrt(n_features)` 分类，`n_features/3` 回归）

#### Boosting 为什么有效？

```
真实值：500      500      500
树1学习：500     500      500
残差  ：  0       0        0      → 学会了，但训练集上的学会不等于泛化

真实值：500      300      800
树1预测: 533     533      533     → 树1很弱，只能预测均值
残差  : -33    -233      267
树2学习: -33    -233      267     → 树2学残差
残差  :  0       0        0
树1+树2: 500     300      800     → 正确
```

> 术语：**Boosting** — 串行训练，每个新模型纠正前一个的残差
> 术语：**残差拟合 residual fitting** — 后一棵树拟合前一棵的残差
> 术语：**学习率/收缩率 shrinkage** — boosting 中每棵树的贡献打折（0.01~0.3），防止过拟合

#### 为什么 GBDT 是"梯度"提升？

```python
# 损失 = MSE = (y - F(x))²
# 梯度 ∂Loss/∂F = -(y - F(x)) = -残差

# 所以：
# "拟合负梯度" = "拟合残差"
# GBDT 每轮拟合损失函数的负梯度方向
```

> 术语：**GBDT（梯度提升决策树）** — 每轮拟合梯度的 Boosting
> 术语：**梯度提升 Gradient Boosting** — 用梯度指导每棵树学什么

#### XGBoost vs LightGBM vs CatBoost（竞赛三宝）

| | XGBoost | LightGBM | CatBoost |
|---|---|---|---|
| 树生长 | 按层 level-wise | **按叶 leaf-wise**（更快） | 对称树 |
| 特征分桶 | 排序后分割 | **直方图**（更省内存） | 直方图 |
| 类别特征 | 需手动编码 | 需手动编码 | **自动处理** |
| 正则化 | L1+L2 | L1+L2 | L2 |
| 缺失值 | 自动学习方向 | 自动处理 | 自动处理 |

> 术语：**XGBoost** — 2014 年陈天奇提出，GBDT 的工程极致版
> 术语：**LightGBM** — 2017 年微软提出，GOSS（梯度单边采样）+ EFB（互斥特征捆绑）
> 术语：**CatBoost** — 2017 年 Yandex 提出，Ordered TS 处理类别特征，Ordered Boosting 防过拟合
> 术语：**level-wise vs leaf-wise** — 一层一层长 vs 只长损失下降最大的叶子
> 术语：**直方图算法 histogram** — 把连续特征离散化成桶，大幅加速分裂搜索

你 mlblack 里的 `TreeEnsembleArtifact` 和 `estimator_spec_search` 就管这个家族。

---

## 第四层：神经网络——自动学特征

### 4.1 为什么会有神经网络——告别特征工程

特征工程的本质问题：**人的归纳偏见决定了模型能看到什么**。

```python
# 人觉得"价格 ∝ 面积²"很重要 → 手造了这个特征
# 可如果真正的规律是"价格 ∝ 面积^1.7"呢？人可能永远不会试 1.7 这个指数
```

神经网络的野心：**让模型自己学特征**。你只要给原始数据，不用手造任何特征。

---

### 4.2 感知机——最简单的神经元

```
输入：x₁, x₂, x₃          (面积, 卧室, 房龄)
权重：w₁, w₂, w₃          (每个输入多重要)
                          ↓
            z = w₁x₁ + w₂x₂ + w₃x₃ + b   ← 和线性回归一样
                          ↓
            a = activation(z)             ← 加非线性
                          ↓
                      输出
```

> 术语：**感知机 perceptron** — 单层线性组合 + 阶跃激活
> 术语：**激活函数 activation function** — 引入非线性
> 术语：**ReLU** — `max(0, x)`，最常用，快且缓解梯度消失
> 术语：**sigmoid** — `1/(1+e^(-x))`，输出 0~1，二分类输出层用
> 术语：**tanh** — `(e^x-e^(-x))/(e^x+e^(-x))`，输出 -1~1，比 sigmoid 零中心更好
> 术语：**Leaky ReLU** — `max(0.01x, x)`，解决 ReLU 的"死亡神经元"问题
> 术语：**GELU** — Transformer 标配，`x*Φ(x)`，比 ReLU 更平滑

#### 激活函数的因果链

```
没有激活函数：
  两层线性 = 一层线性（因为 W₂(W₁x) = (W₂W₁)x = W'x）
  100 层也等于 1 层！

sigmoid 的问题：
  当 |x| 很大 → 导数 ≈ 0 → 梯度消失 → 深层学不动
  → ReLU 解决：x>0 区间导数永远是 1

ReLU 的问题：
  x<0 时输出=0，导数=0 → 神经元"死掉"再也不会激活
  → Leaky ReLU 解决：x<0 时给一个小斜率

GELU 的问题：
  比 ReLU 慢一点但更平滑 → Transformer 这种大模型用 GELU
```

---

### 4.3 多层感知机（MLP）

一层不够？堆起来：

```
输入层 (4个神经元)      面积 卧室 房龄 地段
         ↓              ↙  ↓    ↓    ↓   ↘
隐藏层1 (8个神经元)      ○  ○  ○  ○  ○  ○  ○  ○  ← 学习简单模式
         ↓              ↙  ↓    ↓    ↓   ↘
隐藏层2 (8个神经元)      ○  ○  ○  ○  ○  ○  ○  ○  ← 组合简单模式
         ↓              ↙  ↓    ↓    ↓   ↘
输出层 (1个神经元)       ○                           ← 最终预测
```

> 术语：**MLP（多层感知机）** / **全连接网络 FC / Dense** — 最基本的神经网络
> 术语：**隐藏层 hidden layer** — 输入输出之间的层
> 术语：**宽度 width** — 每层神经元数
> 术语：**深度 depth** — 层数
> 术语：**前馈网络 feedforward network** — 数据单向流动，无循环
> 术语：**全连接 fully connected** — 每层每个神经元连接到下层所有神经元

你 mlblack 的 `NumpyMLP` 和 `numpy_mlp` codec 就对应这里。

---

### 4.4 反向传播——怎么训练 MLP

问题：输出层知道误差，但隐藏层的神经元怎么知道自己该往哪调？

答案：**链式法则**。误差从输出层逐层"传回去"。

```
前向：input → h1 → h2 → output → loss
反向：loss → ∂loss/∂output → ∂output/∂h2 → ∂h2/∂h1 → ∂h1/∂input
        每一步都用"上一层传下来的梯度" × "自己的导数"
```

> 术语：**前向传播 forward pass** — 数据从输入流到输出，得到预测
> 术语：**反向传播 backpropagation** — 从输出往回算梯度，链式法则
> 术语：**梯度 gradient** — 损失对每个参数的偏导数，指向"最陡上升方向"
> 术语：**计算图 computational graph** — 前向过程的 DAG 记录，反向沿图传播梯度
> 术语：**自动微分 autograd / automatic differentiation** — 自动算梯度，不需要人手推公式

#### 一个具体的反向传播推导

```python
# 前向：
z = w₁*x₁ + w₂*x₂ + b    # 线性
a = sigmoid(z)            # 激活
L = (a - y)²              # 损失

# 反向（链式法则）：
∂L/∂a = 2(a-y)                    # 从损失到激活
∂a/∂z = a*(1-a)                   # sigmoid 的导数
∂L/∂z = 2(a-y) * a*(1-a)          # 链式：传到 z
∂z/∂w₁ = x₁                        # 线性对 w₁ 的导数
∂L/∂w₁ = 2(a-y) * a*(1-a) * x₁    # 链式：传到 w₁ ✓
```

> 术语：**学习率 learning rate** — 每步沿梯度走多远
> 术语：**随机梯度下降 SGD** — 每次用一小批数据算梯度（而非全量）
> 术语：**小批量 mini-batch** — 每次用 32/64/128 条数据算梯度
> 术语：**epoch** — 遍历所有训练数据一次
> 术语：**迭代 iteration / step** — 处理一个 batch 算一次梯度 = 一步
> 术语：**全批量 GD** — 全量数据算梯度（慢但准）
> 术语：**在线 SGD** — batch_size=1（极快但极震荡）

#### 为什么是 SGD 而不是 GD？

```
数据集 100 万条：
  GD：100万条算一次 → 一天只能更新一步 → 太慢
  SGD(batch=32)：32条算一次 → 一秒更新几百步 → 够快

SGD 的梯度虽然有噪声，但"期望方向≈正确方向"
```

> 术语：**梯度噪声 gradient noise** — 小批量引入的随机性，反而帮助跳出局部最优

MLBlack 的 `TorchEvaluationProvider` 负责 autograd 证据，NSGABlack 的 `GradientOptimizerAdapter` 负责更新策略。

---

### 4.5 优化器的进化谱系

SGD 有一个问题：**所有参数用同一个学习率**。如果有的参数需要大步、有的需要小步呢？

```
SGD (1951)
 │
 ├─→ 加动量 Momentum (1980s)：累加历史梯度，"惯性"
 │     w = w - lr*(v + 当前梯度)
 │     其中 v = β*v + (1-β)*当前梯度
 │
 ├─→ Nesterov (1983)：先按动量走一步，再在那算梯度
 │     "先看看再走" 而非 "走了再说"
 │
 ├─→ Adagrad (2011)：每个参数独立学习率，频繁更新的参数 lr↓
 │     问题：lr 只减不增，后期过于保守
 │
 ├─→ RMSprop (2012)：改 Adagrad，用移动平均替代累加，lr 可升可降
 │
 ├─→ Adam (2014)：动量 + 自适应学习率 = 今天的默认选择
 │     m = β₁*m + (1-β₁)*g       ← 动量（一次矩）
 │     v = β₂*v + (1-β₂)*g²      ← 自适应（二次矩）
 │     w = w - lr * m/(√v + ε)
 │
 └─→ AdamW (2017)：Adam + 解耦的权重衰减（现代标配）
```

> 术语：**动量 Momentum** — 沿历史梯度方向加速，缓解震荡
> 术语：**Nesterov 加速梯度 NAG** — "先窥探再修正"的动量
> 术语：**Adagrad** — 自适应学习率，稀疏特征友好
> 术语：**RMSprop** — 移动平均，解决 Adagrad 学习率衰减过快
> 术语：**Adam** — Adaptive Moment Estimation，最常用的优化器
> 术语：**AdamW** — Adam + 解耦权重衰减，现代神经网络训练标配
> 术语：**学习率衰减 lr schedule** — 训练后期减小学习率
> 术语：**余弦退火 cosine annealing** — 学习率按余弦曲线下降到 0，周期性重启

#### 为什么需要学习率调度？

```
训练初期：模型什么都不会 → 大学习率，大胆探索
训练后期：模型接近最优 → 小学习率，精细调整

大学习率 = 跨过小坑（局部最优），但可能在最优解附近震荡
小学习率 = 精确逼近最优解，但可能卡在局部最优
       → 先大后小
```

> 术语：**预热 warmup** — 训练最开始的几百步，学习率从 0 线性升到目标值
> 术语：**梯度裁剪 gradient clipping** — 梯度超过阈值就截断，防止梯度爆炸

---

### 4.6 初始化

参数初值决定了优化的起点。太差的开局可能导致永远无法收敛。

> 术语：**Xavier/Glorot 初始化** — 使前向和后向传播的方差保持一致（sigmoid/tanh 用）
> 术语：**He/Kaiming 初始化** — Xavier 的 ReLU 适配版（ReLU 砍掉了一半方差）
> 术语：**零初始化** — 全初始化为 0 → 所有神经元对称，梯度也一样 → 永远学不到不同模式（对称性问题）

```
Xavier 直觉：fan_in=100, fan_out=50
  → 方差设为 2/(100+50)，让信号不放大也不衰减

He 直觉：fan_in=100（ReLU 后）
  → 一半输入被砍 → 方差需要加倍 → 2/100
```

---

### 4.7 正则化——防止过拟合的武器库

> 术语：**过拟合 overfitting** — 模型记住训练数据，但不泛化（训练损失↓ 但验证损失↑）
> 术语：**欠拟合 underfitting** — 模型太简单，训练损失和验证损失都高
> 术语：**偏差-方差权衡 bias-variance tradeoff** — 模型复杂度的两面
> 术语：**泛化误差 generalization error** — 测试集上的误差 = 偏差² + 方差 + 不可约噪声

```
偏差（Bias）：模型假设和真实规律的差距
  高偏差 = 模型太简单 = 欠拟合
  例如：用直线拟合抛物线

方差（Variance）：不同训练集训练出的模型差异
  高方差 = 模型太复杂 = 过拟合
  例如：用 100 次多项式拟合 10 个点

目标：找到偏差和方差的最佳平衡点
```

#### L1 正则化（Lasso）

```
Loss = MSE + λ * Σ|wᵢ|

作用：推动很多 w 变成精确的 0 → 自动特征选择
直觉：λ 越大 → 越少的特征被保留 → 越稀疏
```

> 术语：**L1 正则化 / Lasso** — 产生稀疏解，自动特征选择
> 术语：**稀疏性 sparsity** — 大多数参数为 0，只有少数非零

#### L2 正则化（Ridge / 权重衰减）

```
Loss = MSE + λ * Σ(wᵢ)²

作用：把所有 w 往 0 推，但不精确到 0 → 防止某个 w 过大主导模型
直觉：λ 越大 → 所有参数越小 → 模型越平滑 → 越不容易过拟合
```

> 术语：**L2 正则化 / Ridge / 权重衰减 weight decay** — 限制参数幅值，防止大权重
> 术语：**Elastic Net** — L1 + L2 的混合

#### L1 vs L2 形象理解

```
L1：│w│的惩罚 → 倾向于把小的 w 直接压成 0 → 选特征
L2：w² 的惩罚 → 倾向于把大的 w 压小 → 平滑化

一个极端例子：有两个高度相关的特征
  L1 可能只保留一个（另一个变 0）
  L2 可能两个都保留但都缩小
```

#### Dropout

```
训练时：每步随机"关闭" 20%~50% 的神经元
推理时：所有神经元都激活，但输出乘以 (1-dropout_rate)

↓

等效于：训练了 2^N 个不同的子网络（N=神经元数）
→ 测试时相当于这 2^N 个子网络投票 → 隐式集成
```

> 术语：**Dropout** — 训练时随机丢弃神经元，等效于集成
> 术语：**Dropout rate** — 丢弃概率，通常 0.2~0.5

#### Batch Normalization

```
隐藏层输出可能漂移到激活函数的饱和区（sigmoid 两端梯度≈0）

BatchNorm：
  μ = batch内均值
  σ = batch内标准差
  x̂ = (x-μ)/σ            ← 标准化到均0方1
  y = γ*x̂ + β             ← 可学习的缩放和偏移

好处：
  1. 加速训练（梯度不消失）
  2. 允许更大学习率
  3. 轻微正则化（batch 统计量有噪声）
  4. 减少对初始化的敏感度
```

> 术语：**BatchNorm BN** — 在 mini-batch 内归一化
> 术语：**LayerNorm LN** — 在特征维度归一化（Transformer 标配，不依赖 batch）
> 术语：**InstanceNorm** — 单样本单通道归一化（风格迁移用）
> 术语：**GroupNorm** — BN 和 LN 的折中（小 batch 时替代 BN）
> 术语：**内部协变量偏移 Internal Covariate Shift** — BN 论文提出的动机（虽然后来有争议，但 BN 就是有效）

#### 早停 Early Stopping

```
训练损失：一直在降 ↓↓↓
验证损失：先降 ↓ 再升 ↑ （从这开始过拟合）

        验证损失
           \
            \   /‾‾‾‾‾‾‾
             \ /
              * ← 最佳停止点
```

> 术语：**早停 early stopping** — 验证损失不再下降就停
> 术语：**patience** — 容忍多少个 epoch 不改善才停

#### 数据增强 Data Augmentation

> 术语：**数据增强 data augmentation** — 从现有数据创造新数据
> - 图像：翻转、旋转、裁剪、色变、CutOut、MixUp、CutMix
> - 文本：同义词替换、回译、随机删除/交换
> - 表格：SMOTE（合成少数类样本）

---

### 4.8 批大小的影响

```python
batch_size = 1     → 每个样本更新一次，极震荡，一个 epoch N 次更新
batch_size = 32    → 每 32 个样本更新一次，适度震荡
batch_size = 1024  → 每 1024 个样本更新一次，平滑但慢
batch_size = N     → 全批量 GD，最准但最慢
```

> 术语：**大批量 large batch** — 梯度估计更准，但泛化可能变差（趋向尖锐极小值）
> 术语：**小批量 small batch** — 梯度噪声帮助跳出局部最优，泛化更好
> 术语：**梯度累积 gradient accumulation** — 多步累加梯度再更新，模拟大批量但节省显存

---

## 第五层：卷积网络——处理有空间结构的数据

### 5.1 全连接的困局

一张 224×224×3 的图片如果用全连接：224×224×3 = 150,528 个输入。

如果第一层有 1000 个神经元 → 1.5 亿个参数 → 根本训不动。

更重要的是：**全连接不利用像素之间的空间关系**。左上角的像素和右下角的像素，在全连接看来都一样——这不对。

---

### 5.2 CNN 的核心洞察

```
Insight 1: 局部性 locality
  猫的耳朵不需要关心右下角的背景 → 每次只看一小块

Insight 2: 平移不变性 translation invariance  
  猫耳朵在图片左边还是右边，检测方式应该一样 → 同一组权重扫描整张图
```

> 术语：**CNN 卷积神经网络** — 这两条洞察的工程实现
> 术语：**卷积层 convolution** — 用小窗口（滤波器）扫描整个图片
> 术语：**滤波器 filter / 卷积核 kernel** — 小权重矩阵，检测特定模式
> 术语：**权值共享 weight sharing** — 同一组权重用于所有位置

#### 卷积到底怎么算？

```
输入图片 5×5:          滤波器 3×3:      输出特征图 3×3:
 1  1  1  0  0         1  0  1
 0  1  1  1  0    *    0  1  0    =    4  3  4
 0  0  1  1  1         1  0  1         2  4  3
 0  0  1  1  0                         2  3  4
 0  1  1  0  0

计算第一个位置：
1*1 + 1*0 + 1*1 + 0*0 + 1*1 + 1*0 + 0*1 + 0*0 + 0*1 = 4
```

> 术语：**步长 stride** — 窗口每次移动几个像素
> 术语：**填充 padding** — 图片边缘补零保持尺寸：`same`（输出同尺寸），`valid`（不补）
> 术语：**池化 pooling** — 降采样，减少计算量并提供平移不变性
> 术语：**最大池化 max pooling** — 取窗口最大值
> 术语：**平均池化 average pooling** — 取窗口平均值
> 术语：**全局平均池化 global average pooling** — 整张特征图池化为一个值（替代全连接，大幅减少参数）
> 术语：**感受野 receptive field** — 一个输出神经元"看到"的输入区域
> 术语：**特征图 feature map** — 卷积层的输出（每个通道是一个检测器的响应图）

#### 通道（Channel）是什么？

```
RGB图片：3个通道（红绿蓝）
卷积层1：64个通道 → 每个通道检测不同模式（边缘、颜色、纹理...）
卷积层2：128个通道 → 每个通道组合前一层模式（眼睛、鼻子、嘴巴...）

通道数 = "这个层用了多少种探测器"
```

> 术语：**通道 channel / 深度 depth** — 每层有多少个滤波器
> 术语：**1×1 卷积** — 不改变空间尺寸，只改变通道数（跨通道信息融合 + 降维）

---

### 5.3 CNN 的经典架构谱系

```
LeNet-5 (1998)        手写数字识别，5层，CNN 鼻祖
  ↓
AlexNet (2012)        ImageNet 夺冠，GPU训练，ReLU+Dropout，CNN 复兴
  ↓
VGG (2014)            只用 3×3 卷积，深度 = 好（16/19层）
  ↓
GoogLeNet/Inception (2014)  多尺度并行卷积 + 1×1降维
  ↓
ResNet (2015)         152层！残差连接解决退化问题
  ↓
DenseNet (2017)       每层连接到所有后续层
  ↓
EfficientNet (2019)   自动搜索深度/宽度/分辨率的最优比例
```

> 术语：**残差连接 residual / skip connection** — `output = F(x) + x`
> 术语：**退化问题 degradation** — 不是过拟合，而是层数多了反而训不动（梯度消失+优化困难）
> 术语：**Inception** — 同一层用多种卷积核尺寸（1×1, 3×3, 5×5）并行，让网络自己选
> 术语：**深度可分离卷积 depthwise separable conv** — 先空间卷积再通道卷积，大幅减少参数（MobileNet 核心）
> 术语：**神经架构搜索 NAS** — 用算法自动搜索最优网络结构，而非人手设计

---

## 第六层：序列模型——处理有时间顺序的数据

### 6.1 序列的特殊性

表格数据（房价）：每行独立，顺序无所谓
序列数据（股价、文本、语音）：顺序是核心信息

```
"我 爱 你" ≠ "你 爱 我"
昨天的股价影响今天的股价
```

---

### 6.2 RNN——给网络加"记忆"

核心想法：每个时间步不仅输出预测，还输出一个"隐藏状态"传给下一步。

```
时间步 t:
  h_t = tanh(W_x * x_t + W_h * h_{t-1} + b)     ← 当前输入 + 上一刻记忆
  y_t = softmax(W_y * h_t)                       ← 当前输出
```

> 术语：**RNN（循环神经网络）** — 每步把前一步的隐藏状态传入当前步
> 术语：**隐藏状态 hidden state** — RNN 的"记忆"
> 术语：**循环 recurrent** — 同一个权重矩阵 W_h 在每一步重复使用

#### RNN 怎么展开看

```
展开前（循环视角）：         展开后（时间线）：
   ┌───┐                      
   │   │←───h                 x₀  x₁  x₂  x₃
   │RNN│                       ↓   ↓   ↓   ↓
   │   │───→y                 h₀→h₁→h₂→h₃→...
   └───┘                       ↓   ↓   ↓   ↓
    ↑                         y₀  y₁  y₂  y₃
   h,x
```

---

### 6.3 RNN 的致命问题——梯度消失/爆炸

```
一条 100 步的序列，反向传播要经过 100 步：

∂L/∂h₀ = ∂L/∂h₁₀₀ * ∂h₁₀₀/∂h₉₉ * ... * ∂h₁/∂h₀

如果每一步的导数都 < 1（比如 sigmoid=0.25）：
  0.25¹⁰⁰ ≈ 6×10⁻⁶¹ → 梯度几乎为 0 → 前几十步根本学不到东西

如果每一步的导数都 > 1：
  爆炸 → 无法训练
```

> 术语：**梯度消失/爆炸 vanishing/exploding gradient** — RNN 的核心瓶颈
> 术语：**截断 BPTT truncated BPTT** — 只回传固定步数，缓解梯度消失

---

### 6.4 LSTM——给记忆加门控

LSTM 的洞察：不是所有信息都该被记住，不是所有记忆都该被忘记。

```
LSTM 一个单元的"三条门"：

遗忘门：sigmoid → [0,1] → 旧记忆的每个值要保留多少
  f_t = σ(W_f * [h_{t-1}, x_t])

输入门：sigmoid → 新信息哪些要加入记忆
  i_t = σ(W_i * [h_{t-1}, x_t])

候选记忆：tanh → 新的候选记忆内容
  C̃_t = tanh(W_c * [h_{t-1}, x_t])

更新记忆：遗忘 + 新增
  C_t = f_t * C_{t-1} + i_t * C̃_t        ← 核心公式

输出门：从当前记忆筛选输出
  o_t = σ(W_o * [h_{t-1}, x_t])
  h_t = o_t * tanh(C_t)                   ← 当前隐藏状态
```

**直觉**：
- 遗忘门说：把"昨天"的记忆扔掉无关的
- 输入门说：把"今天"的重要信息存进去
- 输出门说：当前记忆中哪些跟当前输出有关

> 术语：**LSTM（长短期记忆）** — 加了遗忘门、输入门、输出门控制记忆
> 术语：**细胞状态 cell state C_t** — LSTM 的长期记忆通道（就像传送带，信息沿它直通）
> 术语：**GRU（门控循环单元）** — LSTM 简化版，合并遗忘门和输入门成"更新门"，参数更少

---

### 6.5 双向 RNN / BiLSTM

有些任务需要看上下文——比如翻译时一个词的意思可能由后面决定。

```
"我 __ 苹果" 
如果后面是"吃" → __ 可能是"喜欢"
如果后面是"看到" → __ 还是"喜欢"

BiLSTM：一个前向 LSTM + 一个反向 LSTM，输出拼接
```

> 术语：**双向 RNN / BiRNN** — 同时从前和后处理序列
> 术语：**深层 RNN / stacked RNN** — 多层 RNN 堆叠，每层输出作为下层输入

---

### 6.6 Seq2Seq——两个 RNN 连起来

机器翻译：输入"我爱机器学习" → 输出"<START> I love machine learning <END>"

```
编码器 RNN：
  "我"→"爱"→"机器"→"学习" → 最后隐藏状态 = 代表整个句子的向量（上下文向量）

解码器 RNN：
  上下文向量 → "I" → "love" → "machine" → "learning" → <END>
```

> 术语：**编码器-解码器 Encoder-Decoder / Seq2Seq** — 编码器压缩、解码器生成
> 术语：**上下文向量 context vector** — 编码器最终隐藏状态，压缩了整个输入
> 术语：**教师强制 teacher forcing** — 训练解码器时用真实目标词而非模型预测词作为输入

#### Seq2Seq 的瓶颈

把"我爱北京天安门，天安门上太阳升"压缩成一个 512 维的向量。信息丢失不可避免。

---

### 6.7 Attention——让解码器回头看编码器

不再只用最后一个隐藏状态，而是让解码器的每一步**动态地**关注编码器的所有步骤。

```
解码第 1 步生成 "I"：
  计算"和前 4 个编码器状态的相似度"：
    s₁ = 和"我"的相似度 = 0.7  ← 最高
    s₂ = 和"爱"的相似度 = 0.1
    s₃ = 和"机器"的相似度 = 0.1
    s₄ = 和"学习"的相似度 = 0.1
    
  加权和 = 0.7*enc₁ + 0.1*enc₂ + 0.1*enc₃ + 0.1*enc₄
  
  解码器输入 = [上一刻输出, 加权和]  → 预测 "I"
```

> 术语：**注意力机制 Attention** — 解码时动态关注输入的不同部分
> 术语：**Bahdanau Attention / Additive Attention** — 最早的 Attention 机制
> 术语：**Luong Attention / Multiplicative Attention** — 用点积算相似度，更简单更快
> 术语：**对齐 alignment** — Attention 权重表示了"输出词对齐到哪些输入词"
> 术语：**全局注意力 vs 局部注意力** — 关注所有位置 vs 只看一个窗口

---

## 第七层：Transformer——当前一切的基石

### 7.1 RNN 的根本问题

RNN 必须**顺序**执行。第 t 步等第 t-1 步的结果。1000 个词就需要 1000 步计算，无法并行。

同时：长距离依赖仍然困难——即使有 Attention 的 Seq2Seq，编码器本身还是 RNN。

Transformer 的答案：**完全不要循环，纯 Attention。**

---

### 7.2 Self-Attention——序列中每个词看所有词

```
Self-Attention 的一步（以 "The cat sat on the mat" 为例）：

每个词生成三个向量：
  Q (Query)：  "我在找什么相关信息？"
  K (Key)：    "我这里有什么信息？"  
  V (Value)：  "我的实际内容是什么？"

"The cat sat on the mat"
  Q_cat · K_cat = 高（相关）
  Q_cat · K_the = 中  
  Q_cat · K_mat = 高（cat 和 mat 有关系！）

注意力分数 = QK^T / √d_k  →  softmax  →  加权 V
```

> 术语：**自注意力 Self-Attention** — 序列中每个位置关注所有位置（包括自己）
> 术语：**QKV（Query-Key-Value）** — 注意力的计算三件套
> 术语：**缩放因子 √d_k** — 防止点积过大导致 softmax 梯度消失
> 术语：**注意力权重 attention weights** — softmax 后的概率分布，表示每个位置有多"被关注"
> 术语：**注意力图 attention map** — 所有权重的矩阵，可视化后能看到句法关系

#### 一个直观的类比

```
你走进图书馆找书（这是 Query）
每本书的索书号标签（这是 Key）  
每本书的内容（这是 Value）

操作：把你的 Query 和所有 Key 比对 → 找到最相关的书 → 拿 Value（内容）
Self-Attention 就是：每个词同时是"找书的人"和"被找的书"
```

---

### 7.3 多头注意力 Multi-Head Attention

不用一组 QKV，用多组并行，让模型从多个角度理解关系：

```
头1：可能关注"语法关系"（冠词-名词）
头2：可能关注"语义关系"（同义词、相关概念）
头3：可能关注"位置关系"（相邻词）
...
头8：可能关注"长距离依赖"（代词-先行词）

所有头的输出拼接 → 线性投影 → 最终输出
```

> 术语：**多头注意力 Multi-Head Attention** — 多组 QKV 并行计算，关注不同模式
> 术语：**头数 num_heads** — 并行注意力的组数（Transformer-base=8, large=16）

---

### 7.4 位置编码——Transformer 没有"顺序"

RNN 天然知道顺序（词一个一个进）。Transformer 所有词同时进，没有顺序信息。

**解决**：给每个位置加一个独特的"位置签名"。

> 术语：**位置编码 Positional Encoding** — 注入位置信息
> 术语：**正弦位置编码 sinusoidal** — 原论文用正弦/余弦函数，固定不变
> 术语：**学习位置编码 learned positional encoding** — 把位置当可学习参数（BERT/GPT 用）
> 术语：**相对位置编码 relative positional encoding** — 不关心绝对位置，只关心两个词的相对距离
> 术语：**旋转位置编码 RoPE** — 通过旋转矩阵编码位置（LLaMA/Qwen 用，更好的外推性）

---

### 7.5 Transformer 的完整结构

```
输入: "The cat sat on the mat"

  [Embedding + 位置编码]
          ↓
  ┌───────────────────────────────┐
  │  Multi-Head Self-Attention    │  ← 每个词看所有词
  │         + Residual + LN       │
  ├───────────────────────────────┤
  │  Feed-Forward Network (MLP)   │  ← 两个线性层+激活，每个词独立处理
  │         + Residual + LN       │
  └───────────────────────────────┘
          ↓  (重复 N 次，base=6, large=12)
  输出表示（每个位置的向量）
```

> 术语：**Transformer 块/层** — 一个 Attention + 一个 FFN 为一个块
> 术语：**前馈网络 FFN** — 每个 Transformer 层里的两层 MLP（中间维度通常是 4x）
> 术语：**残差连接 Residual** — `LayerNorm(x + Sublayer(x))` —— 原论文用 Post-LN
> 术语：**Pre-LN vs Post-LN** — LayerNorm 在子层前还是后。Pre-LN 训练更稳定，GPT/LLaMA 在用
> 术语：**d_model** — 隐藏维度（base=512, large=768）
> 术语：**d_ff** — FFN 中间维度（通常是 d_model × 4）

---

### 7.6 Transformer 的训练

> 术语：**掩码 Mask** — 防止看到未来信息（解码器自回归训练时），用上三角矩阵屏蔽
> 术语：**填充掩码 padding mask** — 防止关注 padding 位置（不同长度 batch 补 0）
> 术语：**因果掩码 causal mask** — 确保位置 i 只看 ≤i 的位置（GPT 类用）
> 术语：**教师强制 Teacher Forcing** — 训练解码器时用真实目标作为输入
> 术语：**标签平滑 Label Smoothing** — 不用 0/1 标签，用 0.9/0.1，防止过于自信
> 术语：**Warmup Steps** — 学习率先线性增长再衰减（Transformer 训练标配，前 4000 步预热）
> 术语：**梯度累积 gradient accumulation** — 多 step 累加梯度再更新

---

### 7.7 BERT vs GPT vs T5——同一个 Transformer 的三条路径

```
完整的 Transformer：
┌──────────────┐    ┌──────────────┐
│   Encoder    │ →→→│   Decoder    │     (原始翻译用)
│ (双向注意)   │    │ (单向注意)   │
└──────────────┘    └──────────────┘

BERT：只用 Encoder（双向理解）
GPT：  只用 Decoder（自回归生成）
T5：   Encoder + Decoder（Seq2Seq）
```

#### BERT（2018）

```
原理：Masked Language Model
  输入:  "The [MASK] sat on the mat"
  目标:  预测 [MASK] = "cat"

  因为看了前后文（双向）→ 能理解上下文

预训练任务：
  1. Masked LM：15% 的词被随机替换为 [MASK]，猜回来
  2. Next Sentence Prediction：判断两句话 A 和 B 是否相邻
  
使用方式：
  预训练(贵，Google帮你做了) → 微调(便宜，加一个分类头训练几分钟)
```

> 术语：**掩码语言模型 Masked LM (MLM)** — BERT 的预训练任务
> 术语：**下一句预测 Next Sentence Prediction (NSP)** — BERT 的辅助任务（后来证明作用有限）
> 术语：**[CLS] token** — BERT 在每个输入开头加的特殊 token，其输出向量代表整个句子
> 术语：**[SEP] token** — 分隔两个句子的特殊 token
> 术语：**RoBERTa** — BERT 的强力版：去掉 NSP，更大 batch，更多数据，动态 Mask
> 术语：**ALBERT** — 用参数共享减少 BERT 参数量
> 术语：**DistilBERT** — 知识蒸馏压缩 BERT，保留 97% 性能，减少 40% 参数

#### GPT（2018 → 至今）

```
原理：Autoregressive LM（自回归语言模型）
  输入:  "The cat sat on the"
  目标:  预测下一个词 "mat"

  因为只看左边 → 自然适合生成任务

GPT-1 (2018)：证明了"预训练+微调"范式的威力
GPT-2 (2019)：1.5B参数，证明了零样本能力（不给示例也能做任务）
GPT-3 (2020)：175B参数，证明了涌现能力，引入 In-Context Learning
InstructGPT (2022)：GPT-3 + RLHF → 能听懂人类指令
ChatGPT (2022)：InstructGPT 的产品化
GPT-4 (2023)：多模态（文本+图像），推理能力飞跃
GPT-o1 (2024)：推理时搜索（强化推理链），数学/编程大幅提升
```

> 术语：**自回归 autoregressive** — 用历史预测未来，一步步生成
> 术语：**因果语言模型 causal LM** — 只看左边的语言模型 = 自回归
> 术语：**提示 Prompt** — 给 LLM 的输入指令
> 术语：**上下文学习 In-Context Learning (ICL)** — 在 prompt 里给示例，模型直接学会（不改参数）
> 术语：**思维链 Chain-of-Thought (CoT)** — 让模型逐步推理："让我们一步步想"
> 术语：**少样本 few-shot** — 给 K 个示例
> 术语：**零样本 zero-shot** — 不给示例，直接问

#### T5（2019）

```
统一思想：所有 NLP 任务都转化为 "文本→文本"

翻译：  "translate English to German: Hello" → "Hallo"
分类：  "sentiment: I love this movie" → "positive"
问答：  "question: What is 2+2? context: ..." → "4"
```

> 术语：**T5** — Text-to-Text Transfer Transformer
> 术语：**span corruption** — T5 的预训练：随机遮盖连续的若干个 token，而非单个

---

### 7.8 ViT——Transformer 处理图像

> 术语：**ViT（Vision Transformer）** — 把图片切成 16×16 的小块（patch），当"单词"处理
> 术语：**Patch Embedding** — 把每个 image patch 投影到向量
> 术语：**[CLS] token** — 类似 BERT，用于图片分类

```
图片 224×224 → 切 14×14 个 16×16 patch → 196 个"视觉单词" → 输入 Transformer
```

**为什么 ViT 可能超越 CNN？** CNN 的归纳偏置（局部连接、平移不变性）既是加速器也是枷锁。Transformer 没有这些预设，只靠数据量学。数据越多 → 越强。

> 术语：**归纳偏置 inductive bias** — 模型内置的对"好假设"的假设
> 术语：**Swin Transformer** — 加窗注意力的 ViT 变体（分层、局部窗口），效率更好
> 术语：**DETR** — 用 Transformer 做目标检测
> 术语：**MAE（Masked Autoencoder）** — 类似 BERT，随机遮住 75% 的 patch，让模型重建

---

### 7.9 开源大模型的崛起

```
LLaMA (Meta, 2023.02)
  ├── LLaMA-2 (2023.07) — 2T tokens 训练，商业友好
  ├── LLaMA-3 (2024.04) — 8B 和 70B，15T tokens
  └── LLaMA-3.1 (2024.07) — 405B 开源旗舰

Mistral (2023.09) — 7B MoE（每次只用 2B），高效
  └── Mixtral 8×7B — MoE，8个专家每次激活2个

Qwen (阿里, 2023-24) — 多尺寸多语言
  └── Qwen2.5 — 72B 旗舰

DeepSeek (2024-25)
  ├── DeepSeek-V2 — MoE + MLA（多头潜注意力）
  ├── DeepSeek-V3 — MoE + FP8 训练
  └── DeepSeek-R1 — 推理增强，CoT 蒸馏
```

> 术语：**MoE（混合专家 Mixture of Experts）** — 模型中有多个"专家"子网络，每个 token 只激活少数几个 → 大但省计算
> 术语：**门控网络 gating network / Router** — 决定每个 token 走哪几个专家
> 术语：**Grouped Query Attention (GQA)** — 多个 Query 头共享一个 Key-Value 头，省显存

---

### 7.10 Tokenizer——文本怎么变成数字

```
"The cat sat on the mat"
  → ["The", "cat", "sat", "on", "the", "mat"]    ← 按空格切（最简单，词汇表太大）
  → ["The", "cat", "sat", "on", "the", "mat"]    ← 按词切（OOV问题：新词不认识）
  → ["The", "cat", "sat", "on", "the", "mat"]    ← 子词切（BPE/SentencePiece）
```

> 术语：**Token** — 文本被切分成的最小单位
> 术语：**Tokenizer** — 文本→token 的工具
> 术语：**BPE（Byte Pair Encoding）** — 从字符开始，反复合并高频字符对（GPT 用）
> 术语：**WordPiece** — 类似 BPE，但按似然选合并（BERT 用）
> 术语：**SentencePiece / Unigram** — 从大词汇表出发逐步剪枝（LLaMA/T5 用）
> 术语：**词汇表 vocab** — token → id 的映射表
> 术语：**OOV（Out-of-Vocabulary）** — 不在词汇表中的词（子词分词基本解决）
> 术语：**上下文窗口 context window** — 模型一次能看的最大 token 数（4K→128K→1M）

---

### 7.11 规模法则——为什么"更大=更好"

> 术语：**规模法则 Scaling Law** — 模型性能∝参数量^α × 数据量^β × 计算量^γ，可预测
> 术语：**计算最优 compute-optimal** — 给定算力预算，参数量和数据的黄金比例
> 术语：**Chinchilla 缩放定律** — DeepMind 发现：之前各家数据喂少了，数据量应该和参数量同步增长
> 术语：**涌现能力 Emergence** — 小模型做不到，模型大到一定程度突然能做到（推理、代码、多步规划）
> 术语：**对齐 Alignment** — 让模型行为符合人类期望（安全、有用、诚实）
> 术语：**红队测试 red-teaming** — 故意找漏洞/危险输出，测试对齐效果

#### Chinchilla 定律的结论

```
以前(GPT-3)：175B 参数，300B tokens → 参数太多，数据太少
最优(Gopher→Chinchilla)：70B 参数，1.4T tokens → 平衡 → 性能持平但只需 40% 算力

结论：对给定算力预算——数据量 × 20，参数量 / 4
```

---

## 第八层：怎样让 LLM 有用

### 8.1 微调——在预训练基础上继续训练

> 术语：**微调 Fine-tuning** — 在预训练模型上用任务数据继续训练
> 术语：**预训练 pre-training** — 在海量无标注数据上训练（自监督）
> 术语：**下游任务 downstream task** — 模型微调后要解决的具体任务
> 术语：**全量微调 Full Fine-tuning** — 所有参数都更新（显存贵）
> 术语：**指令微调 instruction tuning** — 用"指令-回答"对训练，让模型学会跟随指令
> 术语：**多任务微调 multi-task fine-tuning** — 混合多种任务数据一起微调

#### 参数高效微调（PEFT）——只训练一小部分参数

全量微调 7B 模型需要 28GB 显存（7B × 4 bytes × 模型+优化器状态+梯度 ≈ 84GB）。大多数人不具备这个条件。

> 术语：**PEFT（Parameter-Efficient Fine-Tuning）** — 只更新少量参数

##### LoRA

```
原始的 W (d×k 矩阵，冻结)
  +
ΔW = A(d×r) × B(r×k)  其中 r << min(d,k)
        ↑ 只训练这两个小矩阵

r=8 时：可训练参数 ≈ 原始 × r/min(d,k) ≈ 0.1%
显存需求：原来的约 1/3 → 一块消费级显卡就能微调 7B 模型
```

> 术语：**LoRA（Low-Rank Adaptation）** — 每层加低秩矩阵，只训练这些
> 术语：**秩 rank (r)** — 低秩矩阵的中间维度，越小越省但能力越弱（典型值 8~64）
> 术语：**α 缩放因子** — LoRA 的缩放参数，控制 ΔW 的贡献幅度
> 术语：**QLoRA** — LoRA + 基础模型 4-bit 量化 + 分页优化器 → 单卡 24GB 微调 65B 模型
> 术语：**DoRA** — Weight-Decomposed LoRA，解耦幅度和方向

##### 其他 PEFT 方法

> 术语：**Adapter** — 在 Transformer 层之间插入小模块（几 MB），只训练这些
> 术语：**Prefix Tuning** — 只在输入前加可学习的虚拟 token，训练这些 token 的 embedding
> 术语：**P-Tuning v2** — 每层都加可学习 prompt（类似 Prefix Tuning 的加强版）
> 术语：**IA³** — 只训练三个缩放向量的逐元素乘积，最快最省

---

### 8.2 不改参数的方法

> 术语：**Prompt Engineering** — 写更好的 prompt
> 术语：**少样本 few-shot prompting** — 在 prompt 里给 K 个示例（通常 2~8 个）
> 术语：**零样本 zero-shot prompting** — 不给示例，直接提问
> 术语：**思维链 CoT prompting** — 在 prompt 中加入推理步骤
> 术语：**零样本 CoT** — 加一句"Let's think step by step"
> 术语：**自洽性 self-consistency** — 多次采样 CoT，投票选最一致的答案
> 术语：**思路树 Tree-of-Thought** — 维护多条推理路径，回溯探索
> 术语：**ReAct** — 推理(Reasoning) + 行动(Acting) 交替的 prompt 模式（搜索/计算/调 API）
> 术语：**角色扮演 role prompting** — "你是一个翻译专家..."

---

### 8.3 RAG——检索增强生成

**为什么 RAG 成为标配？**

| 问题 | RAG 怎么解决 |
|---|---|
| LLM 知识有截止日期 | 实时检索最新信息 |
| LLM 会幻觉（编造） | 用外部文档限制生成，减少幻觉 |
| LLM 不知道私有数据 | 把你的文档库向量化，检索后喂给 LLM |
| 全量微调太贵 | 不动模型参数，"外挂"知识 |

#### RAG 全流程

```
离线阶段：
  文档库 → 切块(Chunking) → Embedding模型 → 向量数据库

在线查询：
  用户问题 → Embedding → 在向量DB检索Top-K → 
  拼入 Prompt："{context}\n\n问题：{question}" → LLM 生成答案
```

> 术语：**RAG（Retrieval-Augmented Generation）** — 检索增强生成
> 术语：**Embedding** — 文本→向量，语义相似的文本=向量距离近
> 术语：**分块 chunking** — 把长文档切成小段（512-1024 tokens）
> 术语：**重叠 overlap** — 相邻块之间重复 10-20% 的内容，防止信息在边界断开
> 术语：**向量数据库 Vector DB** — FAISS, Milvus, Pinecone, Chroma, Qdrant
> 术语：**近似最近邻搜索 ANN** — 快速找最相似向量（HNSW, IVF, PQ）
> 术语：**检索器 Retriever** — 检索模块
> 术语：**稀疏检索 BM25** — 基于词频的检索（不需要 embedding）
> 术语：**稠密检索 Dense Retrieval** — 基于 embedding 的语义检索
> 术语：**混合检索 Hybrid Search** — BM25 + Dense，取长补短
> 术语：**重排序 Re-ranker** — 用更强的模型对检索结果二次排序
> 术语：**上下文窗口 context window** — LLM 能接收的总 token 数，限制了能塞多少文档
> 术语：**Lost in the Middle** — LLM 容易忽略中间位置的文档，首尾的信息更被关注

#### RAG 进阶

> 术语：**GraphRAG** — 用知识图谱组织文档间关系，而非独立 chunks
> 术语：**Agentic RAG** — 多步检索：读→发现缺信息→再检索→再读
> 术语：**Self-RAG** — 模型自己判断是否需要检索、检索结果是否相关
> 术语：**Corrective RAG** — 检索后评估质量，不相关的扔掉重检

---

### 8.4 MoE——同样的参数总量，更少的计算

```
Dense Model：每个 token 经过所有参数（例如 70B 全部激活）

MoE Model：   模型总参数量很大（例如 8×7B=56B）
              但每个 token 只走 2 个专家 ≈ 14B 激活参数
              
              总参数多（知识容量大）+ 激活参数少（推理快）
              = 鱼和熊掌兼得
```

> 术语：**专家 Expert** — MoE 中的子网络（通常是独立的 FFN）
> 术语：**门控/路由 Router/Gate** — 决定每个 token 分配去哪几个专家
> 术语：**Top-K 路由** — 每个 token 选 top-K 个专家（K 通常为 2）
> 术语：**负载均衡 Load Balancing** — 防止所有 token 都涌去同一个专家
> 术语：**辅助损失 auxiliary loss** — 用来鼓励路由均匀分布的额外损失项

---

### 8.5 KV Cache——生成加速的核心

自回归生成每次都要算所有历史 token 的 attention：

```
第 1 步："The" → 算 1 个 token 的 attention → 预测 "cat"
第 2 步："The cat" → 重算前 2 个 token 的 attention → 预测 "sat"

KV Cache 的做法：
  第 1 步的 K,V 存起来
  第 2 步：直接用缓存的旧 K,V + 只算新 token "cat" 的 K,V
         → 节省了"The"和"cat"的重复计算
```

> 术语：**KV Cache** — 缓存已生成 token 的 Key 和 Value，避免重复计算
> 术语：**prefill 阶段** — 处理输入 prompt 的所有 token（并行，算力密集）
> 术语：**decode 阶段** — 逐 token 生成（串行，显存带宽密集）
> 术语：**Flash Attention** — 利用 GPU 内存层次，分块计算 Attention，加速 2-4x + 省显存
> 术语：**PagedAttention** — 把 KV Cache 按页管理（类似操作系统虚拟内存），vLLM 核心
> 术语：**GQA（Grouped Query Attention）** — 多个 Query 头共享 KV 头，大幅减少 KV Cache

---

### 8.6 推理优化

> 术语：**vLLM** — 高吞吐 LLM 推理引擎（PagedAttention）
> 术语：**TensorRT-LLM** — NVIDIA 的 LLM 推理优化（算子融合、量化）
> 术语：**投机解码 speculative decoding** — 用小模型快速草拟→大模型验证
> 术语：**连续批处理 continuous batching** — 不等整个 batch 完成，哪个请求做完就替换新请求
> 术语：**推测性解码 speculative decoding** — 小模型猜几个 token，大模型一次验证

---

## 第九层：生成模型——不只预测，还要创造

### 9.1 判别 vs 生成

| | 判别模型 Discriminative | 生成模型 Generative |
|---|---|---|
| 学什么 | P(y \| x) — "给定 x，y 是什么" | P(x, y) 或 P(x) — "x 长什么样" |
| 能做什么 | 分类、回归 | 生成新数据 |
| 例子 | 逻辑回归、SVM、CNN分类 | GAN、VAE、Diffusion、GPT |
| 训练信号 | 有标签 | 不一定需要标签 |

---

### 9.2 VAE——学了分布就能采样

```
Autoencoder：
  x → 编码器 → z(确定的向量) → 解码器 → x̂

VAE：
  x → 编码器 → μ(均值), σ(标准差) → 采样 z~N(μ,σ) → 解码器 → x̂
                         ↑
              学的是"分布"，不是"点"
```

> 术语：**自编码器 Autoencoder** — 编码器→瓶颈→解码器，学习压缩表示
> 术语：**瓶颈层 bottleneck / latent space** — 中间的压缩表示
> 术语：**VAE（变分自编码器）** — 学了分布而非点，能采样
> 术语：**潜在变量 latent variable z** — 数据背后的隐藏因素
> 术语：**KL 散度 KL Divergence** — 衡量学习的分布和标准正态分布的差距 → 正则化
> 术语：**重参数化技巧 reparameterization trick** — `z = μ + σ*ε` (ε~N(0,1))，让梯度能流过采样
> 术语：**ELBO（证据下界）** — VAE 的优化目标 = 重建误差 + KL 散度

---

### 9.3 GAN——两个网络你死我活

```
生成器 G(z)："我造一张假图，想骗过判别器"
判别器 D(x)："我判断这张图是真的还是假的"

训练过程：
  1. 判别器看真图→输出"真"
  2. 生成器造假图→判别器输出"真？假？"
  3. 生成器的损失 = 骗过判别器的程度
  4. 判别器的损失 = 正确判断真假的准确度
  
  互相迭代 → 生成器越来越会造假 → 判别器越来越会识破 → 
  最终生成器造的图以假乱真
```

> 术语：**GAN（生成对抗网络 Generative Adversarial Network）** — 两个网络对抗训练
> 术语：**生成器 Generator** — 从噪声生成假数据
> 术语：**判别器 Discriminator** — 区分真假数据
> 术语：**对抗训练 adversarial training** — 两者互相博弈
> 术语：**纳什均衡 Nash equilibrium** — GAN 收敛的理想状态（判别器输出总是 0.5，分不清了）
> 术语：**模式崩塌 mode collapse** — 生成器学会几种花样就躺平，产出的多样性极低
> 术语：**WGAN** — 用 Wasserstein 距离替代 JS 散度，训练更稳定
> 术语：**梯度惩罚 gradient penalty (WGAN-GP)** — WGAN 的改进，强制满足 Lipschitz 条件
> 术语：**条件 GAN cGAN** — 给生成器和判别器都输入条件 y，能按条件生成
> 术语：**StyleGAN** — 通过风格混合控制生成的内容（人脸属性可编辑）

---

### 9.4 Diffusion Model——一步一步画图

```
思路：学"怎么从纯噪声恢复成图"。

训练：
  给图 x₀ → 一步加噪声 → x₁（多一点噪声）→ x₂ → ... → x_T（纯噪声）
  让模型学：给定噪声图 x_t 和 t，预测加进去的噪声 ε

推理（生成）：
  纯噪声 x_T → 模型去噪 → x_{T-1} → ... → x₀（清晰图片）
```

> 术语：**扩散模型 Diffusion Model** — 逐步加噪、逐步去噪
> 术语：**前向扩散 forward diffusion** — 往图片逐步加高斯噪声直到纯噪声
> 术语：**反向过程 reverse process / 去噪 denoising** — 从噪声逐步恢复图片
> 术语：**噪声预测 noise prediction** — 模型的任务：预测每一步加了多少噪声，而非直接预测图片
> 术语：**噪声调度 noise schedule** — 不同的加噪速度方案（线性、余弦等）
> 术语：**DDPM** — Denoising Diffusion Probabilistic Models，最早的扩散模型
> 术语：**DDIM** — 去噪步数可调的改进（不需要 1000 步）
> 术语：**分数匹配 score matching** — 扩散模型的数学等价视角
> 术语：**Stable Diffusion** — 在潜在空间做扩散（先用 VAE 压缩到 latent，在此做 diffusion）
> 术语：**CFG（无分类器引导 Classifier-Free Guidance）** — 控制"生成和条件多匹配"的程度
> 术语：**text-to-image** — 文本提示→图片（DALL-E, Stable Diffusion, Midjourney）
> 术语：**ControlNet** — 给 Stable Diffusion 加精确控制（线稿、深度图、姿态）
> 术语：**LoRA in Diffusion** — 用少量图微调 Stable Diffusion，学特定风格/人物

#### 为什么 Diffusion > GAN（当前图像生成的选择）？

| | GAN | Diffusion |
|---|---|---|
| 训练难度 | 模式崩塌常见，需要精心调参 | 训练非常稳定 |
| 生成质量 | 可非常高（StyleGAN） | 普遍高 |
| 多样性 | 可能模式崩塌 | 多样性好 |
| 速度 | 快（单步） | 慢（20~50步） |
| 可控性 | 条件GAN | CFG + ControlNet, 非常可控 |

---

### 9.5 Flow Matching——很快

> 术语：**Normalizing Flow** — 可逆变换序列，精确算概率密度
> 术语：**连续归一化流 CNF** — 用 ODE（常微分方程）表示连续变换
> 术语：**Flow Matching** — 直接学一个向量场，沿着它把噪声推到数据
> 术语：**Rectified Flow** — Flow Matching 改进：让路径变成直线，减少采样步数
> 术语：**一致性模型 Consistency Model** — 训练模型直接一步去噪（1步生成！）

**为什么出现这一脉？** 扩散模型虽然好但慢（20~1000步）。Flow Matching/Consistency Model 试图用 1~4 步完成生成。

---

## 第十层：强化学习——从反馈中学

### 10.1 RL 和 ML 根本不同在哪

| | 监督学习（传统 ML） | 强化学习 RL |
|---|---|---|
| 数据 | 静态数据集，有标准答案 | 没有标准答案，只有奖励信号 |
| 反馈 | 每个数据点有标签（正确/错误） | 延迟反馈（下一盘棋才知输赢） |
| 影响 | 模型不影响数据 | 智能体的行动影响后续状态 |
| 核心挑战 | 泛化 | 探索 vs 利用的权衡 |

> 术语：**智能体 agent** — 做决策的实体
> 术语：**环境 environment** — 智能体所处的世界
> 术语：**状态 state s** — 环境的当前情况
> 术语：**动作 action a** — 智能体的决策
> 术语：**奖励 reward r** — 环境给动作的反馈
> 术语：**轨迹 trajectory / episode** — 状态-动作-奖励序列：s₀,a₀,r₁,s₁,a₁,r₂,...
> 术语：**策略 policy π** — 状态→动作的映射，可以是确定性的或者概率性的
> 术语：**值函数 value function V(s)** — 从状态 s 出发的期望累积奖励
> 术语：**Q 函数 Q(s,a)** — 在状态 s 做动作 a 的期望累积奖励
> 术语：**折扣因子 γ** — 未来奖励的衰减系数（今天 100 块 > 一年后 100 块）

---

### 10.2 探索 vs 利用——RL 最核心的矛盾

```
探索 Exploration：试新东西（可能更好可能更差）
  例：路过一家新餐厅

利用 Exploitation：用已知最好的（稳定但不一定最优）
  例：每次都去你最喜欢的那家餐厅

ε-greedy：以 ε 概率随机探索，以 1-ε 概率选最优
  训练初期 ε 大（多探索），后期 ε 小（多利用）
```

> 术语：**探索-利用权衡 exploration-exploitation tradeoff**
> 术语：**ε-greedy** — 最基础的探索策略
> 术语：**UCB（上置信界 Upper Confidence Bound）** — 选择"平均值+不确定性"最高的动作
> 术语：**Thompson Sampling** — 从后验分布采样选动作

---

### 10.3 两大流派：基于价值 vs 基于策略

#### 基于价值 Value-Based

```
学 Q(s,a)：每个（状态，动作）对的值

Deep Q-Network (DQN, 2013)：
  用神经网络近似 Q 函数
  + 经验回放 Experience Replay：存历史经验 → 随机采样训练（打破相关性）
  + 目标网络 Target Network：用单独的慢更新网络计算目标（稳定训练）
```

> 术语：**Q-Learning** — 学习 Q(s,a) 的经典算法
> 术语：**DQN** — Deep Q-Network，Q-Learning + 神经网络
> 术语：**经验回放 Experience Replay** — 存 (s,a,r,s') 到缓冲区，随机采样训练
> 术语：**目标网络 Target Network** — 定期从主网络复制参数
> 术语：**Double DQN** — 解决 DQN 的"过度乐观"问题
> 术语：**Dueling DQN** — 把 Q 拆成 V(s) + A(s,a)，学习更高效

#### 基于策略 Policy-Based

```
不学值函数，直接学策略 π(a|s)

Policy Gradient：
  θ ← θ + α * ▽log π(a|s;θ) * R

  直觉：如果动作导致好结果(R>0)，增加它的概率
        如果动作导致坏结果(R<0)，减少它的概率
```

> 术语：**策略梯度 Policy Gradient** — 直接优化策略参数
> 术语：**REINFORCE** — 最基础的策略梯度算法
> 术语：**基线 baseline** — 减去一个基线减少梯度方差
> 术语：**Actor-Critic** — 用 Critic（值函数）做基线指导 Actor（策略）更新

#### Actor-Critic——两者结合

```
Actor (策略网络 π)： 决定怎么做 ("做这个动作")
Critic (值网络 V/Q)：评估做得好不好 ("你这个动作分数很低")
                       ↓
                  相当于"教练"，批评 Actor 并指导改进
```

> 术语：**A2C/A3C** — Advantage Actor-Critic（同步/异步）
> 术语：**优势函数 advantage A(s,a) = Q(s,a) - V(s)** — "这个动作比平均水平好多少"
> 术语：**PPO（Proximal Policy Optimization）** — 限制策略更新幅度（剪辑目标），RLHF 用的就是这个
> 术语：**TRPO** — PPO 的前身，用约束而非剪辑，复杂
> 术语：**GAE（Generalized Advantage Estimation）** — 平衡偏差和方差的优势估计方法

---

### 10.4 PPO——RLHF 的核心算法

PPO 的核心：**不要让策略一次更新太多**。

```
旧策略 π_old：做动作 a 的概率是 0.3
新策略 π_new：做动作 a 的概率是 0.9

ratio = 0.9/0.3 = 3 → 变化太大，可能破坏训练

PPO 的剪裁：
  clipped_ratio = clip(ratio, 1-ε, 1+ε)
  
  如果 advantage > 0(这个动作好)，ratio 被限制在 1+ε (不会鼓励过度增加)
  如果 advantage < 0(这个动作差)，ratio 被限制在 1-ε (不会惩罚过度减少)
```

> 术语：**PPO** — 剪辑目标函数限制更新
> 术语：**KL 惩罚** — PPO 的另一种变体（用 KL 散度限制而非剪辑）

---

### 10.5 RLHF——让 LLM 懂人类偏好

```
RLHF 三步：

Step 1: 监督微调 SFT
  InstructGPT 用人类写的"指令→好回答"对微调 GPT-3

Step 2: 训练奖励模型 RM
  对同一 prompt，让模型生成多个回答 → 人类排序 →
  训练一个"评分器"来预测人类偏好

Step 3: RL 训练
  用 PPO + 奖励模型训练 SFT 模型
  + KL 惩罚（防止模型偏离 SFT 太远，产生胡言乱语）
```

> 术语：**RLHF（Reinforcement Learning from Human Feedback）** — 人类反馈强化学习
> 术语：**奖励模型 Reward Model (RM)** — 从人类比较数据学的好/坏打分器
> 术语：**SFT（Supervised Fine-Tuning）** — RLHF 第一步，用人类示范微调
> 术语：**偏好数据 preference data** — 人类对多个回答的排序（A > B > C）
> 术语：**DPO（Direct Preference Optimization）** — 跳过奖励模型！直接优化人类偏好
> 术语：**RLAIF** — 用 AI（而非人类）给偏好标注，降低成本
> 术语：**Constitutional AI** — 用一套规则/宪法引导 AI 自我改进（Anthropic 的做法）
> 术语：**ORPO** — 同时做指令跟随和偏好对齐，一步完成 SFT+DPO

---

## 第十一层：其他重要的术语家族

### 11.1 模型压缩和加速

> 术语：**量化 Quantization** — 从 FP32 降到 INT8/INT4/FP8，省显存、加速
> 术语：**训练后量化 PTQ（Post-Training Quantization）** — 训练完再量化，简单但可能掉点
> 术语：**量化感知训练 QAT（Quantization-Aware Training）** — 训练时模拟量化效果，精度损失更小
> 术语：**GPTQ** — 基于 OBQ 的后训练量化，LLM 常用
> 术语：**AWQ（Activation-aware Weight Quantization）** — 保护重要通道的量化
> 术语：**剪枝 Pruning** — 去除不重要的权重/神经元/注意力头
> 术语：**非结构化剪枝 unstructured pruning** — 单个权重稀疏化（hardware support 有限）
> 术语：**结构化剪枝 structured pruning** — 去掉整行/列/头/层（直接加速）
> 术语：**知识蒸馏 Knowledge Distillation** — 大模型(教师)教小模型(学生)
> 术语：**logit 蒸馏** — 学生学教师的输出概率分布
> 术语：**特征蒸馏** — 学生学教师中间层的表示
> 术语：**ONNX** — 开放神经网络交换格式，跨框架部署
> 术语：**推理 inference** — 用训练好的模型做预测
> 术语：**延迟 latency** — 单次预测耗时
> 术语：**吞吐量 throughput** — 单位时间处理量

---

### 11.2 自监督学习——不靠标签也能学

> 术语：**自监督学习 Self-Supervised Learning (SSL)** — 从数据本身构造监督信号

```
对比学习 Contrastive Learning：
  同一张图裁两下 → 这两块应该相似（正样本对）
  不同图的裁块 → 应该不相似（负样本对）
  
  模型学会把同一事物的不同视角映射到相近的向量
```

> 术语：**对比学习 Contrastive Learning** — 拉近正样本、推远负样本
> 术语：**SimCLR** — 简单的对比学习框架（大数据增强 + 大 batch + 投影头）
> 术语：**MoCo** — 动量编码器 + 队列，减少对 batch size 的依赖
> 术语：**BYOL** — 不用负样本！预测目标网络的输出（自蒸馏思路）
> 术语：**SimSiam** — 最简单的孪生网络，停止梯度是唯一关键
> 术语：**掩码自编码器 MAE** — 遮住 75% 的图像 patch → 让模型重建

---

### 11.3 迁移学习——借别人的成果

> 术语：**迁移学习 Transfer Learning** — 把任务 A 学到的知识用到任务 B
> 术语：**源域/任务 source domain/task** — 被借用的领域
> 术语：**目标域/任务 target domain/task** — 要解决的新领域
> 术语：**域适应 domain adaptation** — 源域和目标域分布不同时的适应
> 术语：**域泛化 domain generalization** — 训练时不看目标域，测试时泛化到新域
> 术语：**零样本学习 zero-shot learning** — 训练时没见过的类别，测试时能分类
> 术语：**少样本学习 few-shot learning** — 每个新类别只有几个训练样本

```
传统 ML：每个任务从零训练
迁移学习：ImageNet 预训练 → 冻住前几层 → 只训练后几层（你的数据）
现代 LLM：LLaMA 预训练 → LoRA 微调（你的任务）
```

---

### 11.4 多模态学习——连接不同类型的数据

> 术语：**多模态 Multimodal** — 处理多种数据类型（文本+图像+音频+视频）
> 术语：**CLIP** — 对比学习对齐文本和图像（4亿图文对），理解"苹果"的文字和苹果的图片是同一个东西
> 术语：**多模态 LLM** — GPT-4V, LLaVA, MiniGPT-4（能看图回答问题）
> 术语：**图文对齐 image-text alignment** — CLIP 做的事：文本向量和对应图像向量尽量接近
> 术语：**LLaVA** — 把 CLIP 视觉编码器 + LLM 通过一个投影层连起来
> 术语：**Q-Former** — BLIP-2 的桥接模块，可学习的查询向量提取视觉信息给 LLM

---

### 11.5 图神经网络 GNN

> 术语：**图 Graph** — 节点 + 边的数据结构（社交网络、分子、知识图谱）
> 术语：**GNN（图神经网络）** — 在图结构上做深度学习
> 术语：**GCN（图卷积网络）** — 对邻居做加权平均，类似图上的卷积
> 术语：**消息传递 Message Passing** — GNN 核心机制：节点收集邻居信息→更新自身表示
> 术语：**GAT（图注意力网络）** — 用 Attention 给不同邻居不同权重
> 术语：**GraphSAGE** — 采样邻居的训练方式，可扩展到超大图
> 术语：**节点表示 node embedding** — 学到的每个节点的向量表示
> 术语：**归纳学习 vs 直推学习 inductive vs transductive** — 能推广到新节点 vs 只能处理训练时见过的节点

---

### 11.6 因果推断——相关性 ≠ 因果

> 术语：**因果推断 causal inference** — 从数据推断因果关系
> 术语：**相关 vs 因果 correlation vs causation** — "冰淇淋销量和溺水人数正相关" 不是冰淇淋导致溺水，是气温高了
> 术语：**混杂因子 confounder** — 同时影响 A 和 B 的第三个变量（气温同时影响冰淇淋销量和游泳人数）
> 术语：**平均处理效应 ATE** — 干预后平均多少个体会受影响
> 术语：**do-算子 do-calculus** — Pearl 因果框架的核心，"强行设定 X=x" 而非被动观察
> 术语：**结构因果模型 SCM** — 用有向图 + 方程表示因果关系
> 术语：**工具变量 Instrumental Variable (IV)** — 经济学中的因果推断方法
> 术语：**双重差分 DiD** — 政策评估的因果推断方法
> 术语：**倾向性得分匹配 PSM** — 匹配"相似的"处理组和对照组

---

### 11.7 时间序列

> 术语：**时间序列 time series** — 按时间顺序排列的数据
> 术语：**平稳性 stationarity** — 统计特性（均值、方差）不随时间变化
> 术语：**季节性 seasonality** — 周期性模式（每天/每周/每年重复）
> 术语：**趋势 trend** — 长期上升或下降
> 术语：**ARIMA** — 自回归积分滑动平均，经典统计预测模型
> 术语：**SARIMA** — 加了季节性的 ARIMA
> 术语：**VAR** — 向量自回归，多变量互相预测
> 术语：**Granger 因果** — "加入 X 的历史能更好地预测 Y → X Granger-cause Y"（统计因果，非真因果）
> 术语：**自相关 ACF / 偏自相关 PACF** — 看序列和自身滞后的相关性
> 术语：**STL 分解** — 把序列拆成趋势+季节+残差
> 术语：**时间序列交叉验证** — 不能用随机 K-Fold，必须保持时间顺序

---

### 11.8 异常检测

> 术语：**异常检测 anomaly detection** — 找出不符合正常模式的数据
> 术语：**离群点 outlier** — 极端值
> 术语：**新颖点 novelty** — 训练时没见过的新模式（有点类似开集识别）
> 术语：**孤立森林 Isolation Forest** — 随机分割，异常点更容易被孤立（分割深度浅）
> 术语：**LOF（局部异常因子）** — 看一个点和邻居的密度差异
> 术语：**One-Class SVM** — 只学正常数据，判定新数据是否"和正常数据一样"
> 术语：**自编码器异常检测** — 正常数据的重建误差小，异常重建误差大

---

## 第十二层：运筹优化——你另一半技术栈

### 12.1 和机器学习的深层对应

| 运筹优化 | 机器学习 | 数学本质 |
|---|---|---|
| 目标函数 | 损失函数 | 要最小化的实值函数 |
| 决策变量 | 模型参数 | 要优化的变量 |
| 约束条件 | 正则化/约束 | 必须满足的条件 |
| 可行域 | 参数空间 / 假设空间 | 所有合法解 |
| 最优解 | 最优参数 | 目标最小的参数 |
| 搜索算法 | 优化器 | 找到最优解的方法 |
| 启发式 | 归纳偏置 | 对"好解长什么样"的预设 |

**同一个问题，不同术语，同一套数学。**

---

### 12.2 为什么运筹优化和 ML 会融合

```
ML 的传统假设：数据是 i.i.d. 的，你给我什么数据我学什么
OR 的态度：人类有商业约束、物理限制、多目标权衡

但现实中：
  预测销量只是第一步 → 真正的问题是在仓库容量、运输时效、成本预算约束下
  → 怎么安排生产计划？怎么分配库存？

ML 告诉你"会发生什么"，OR 告诉你在约束下"该怎么做"
两者结合 = 从预测到决策的闭环
```

---

### 12.3 经典优化方法

#### 线性规划 LP

```
目标: min c^T x
约束: Ax ≤ b, x ≥ 0

特点：目标和约束都是线性的
解法：单纯形法 (Simplex)、内点法 (Interior Point)
```

> 术语：**线性规划 LP** — 目标和约束都是线性的
> 术语：**单纯形法 Simplex** — 沿可行域顶点走，经典的 LP 求解法
> 术语：**内点法 Interior Point Method** — 穿过可行域内部逼近最优，大规模 LP 更快
> 术语：**敏感性分析 sensitivity analysis** — 参数变化对最优解的影响
> 术语：**影子价格 shadow price** — 多一单位资源能改善多少目标值

#### 整数规划 IP / MIP

```
LP 但某些/全部变量必须是整数

解法：分支定界 (Branch & Bound)
  1. 先解松弛问题（忽略整数约束，当 LP 解）
  2. 选一个非整数变量 → 分两支（x≤k 和 x≥k+1）
  3. 递归，剪去不可能更好的分支
```

> 术语：**整数规划 IP** — 全部整数变量
> 术语：**混合整数规划 MIP** — 有整数也有连续变量
> 术语：**分支定界 Branch & Bound** — IP 的精确框架
> 术语：**分支切割 Branch & Cut** — B&B + 切平面，商用求解器的核心
> 术语：**对偶 Dual** — 从"另一面"看问题，提供下界（最小化问题）
> 术语：**对偶间隙 duality gap** — 上下界的差距，衡量离最优多远
> 术语：**TSP（旅行商问题）** — 访问所有城市一次且回到起点，最短路径（经典 NP 难）
> 术语：**VRP（车辆路径问题）** — TSP 的多车版

---

### 12.4 元启发式——精确算法不够快时的出路

精确算法太慢（NP 难 → 可能算到世界末日），用近似搜索。

> 术语：**元启发式 Metaheuristic** — 通用近似搜索框架（不针对特定问题）
> 术语：**NP 难 NP-hard** — 没有多项式时间精确算法的优化问题（不一定可验证）
> 术语：**NP 完全 NP-complete** — NP 问题中最难的（可验证多项式时间）

#### 搜索的两极

```
局部搜索 (Local Search)：在当前解附近搜索
  代表：梯度下降、爬山法、模拟退火、邻域搜索
  优点：快
  缺点：可能卡在局部最优

全局搜索 (Global Search)：大范围探索整个搜索空间
  代表：遗传算法、差分进化、粒子群、随机搜索
  优点：不易卡局部最优
  缺点：慢
```

#### 单点搜索

> 术语：**爬山法 Hill Climbing** — 每次接受比当前更好的邻居解
> 术语：**模拟退火 SA（Simulated Annealing）** — 以一定概率接受更差解，概率随时间降低
> 术语：**禁忌搜索 Tabu Search** — 记住近期访问过的解，禁止短期内再访问，避免循环
> 术语：**变邻域搜索 VNS（Variable Neighborhood Search）** — 系统切换不同邻域结构

#### SA 的直觉

```
温度 T 高：可能接受很差的解（像金属加热后分子躁动）→ 探索
温度 T 低：几乎不接受更差解（像冷却后分子稳定）→ 精修

接受概率 = exp(-ΔE / T)
  ΔE=+100(变差), T=100 → P ≈ 0.37 → 接受
  ΔE=+100(变差), T=1   → P ≈ 0    → 拒绝
```

#### VNS 的直觉

```
邻域1：交换两个城市（小改动）
邻域2：反转一段路径（中改动）
邻域3：随机重排一半（大改动）

算法：先在邻域1搜 → 卡住了 → 切到邻域2 → 换个角度看问题
      卡住了 → 切到邻域3 → 再换个角度
```

#### 种群搜索

> 术语：**进化算法 Evolutionary Algorithm (EA)** — 用种群、选择、交叉、变异模拟自然进化
> 术语：**遗传算法 GA（Genetic Algorithm）** — 最早最经典的 EA
> 术语：**差分进化 DE（Differential Evolution）** — 用种群差异指导变异，参数少、效果好
> 术语：**粒子群 PSO（Particle Swarm Optimization）** — 模拟鸟群觅食，个体+全局最优引导
> 术语：**蚁群算法 ACO** — 蚂蚁信息素的正反馈机制，路径优化

#### DE 的直观理解

```
种群有 100 个解

对每个解 i：
  随机取 3 个不同的解 a,b,c
  变异：v = a + F*(b-c)      ← "从 b 到 c 的方向" 就是搜索方向
  交叉：新解 = 混合 v 和 原解 i
  选择：新解更好就替换原解

本质：种群本身提供了"搜索方向"，不需要人为设计邻域
```

#### 多目标优化

> 术语：**多目标优化 Multi-Objective Optimization** — 同时优化多个互相冲突的目标
> 术语：**帕累托最优 Pareto Optimal** — 改善了A就会恶化B的解（没有绝对更好，只有权衡）
> 术语：**帕累托前沿 Pareto Front** — 所有帕累托最优解构成的前沿面
> 术语：**支配 Dominance** — A 在所有目标 ≥ B 且至少一个 > B，则 A 支配 B
> 术语：**拥挤度 Crowding Distance** — 衡量解在帕累托前沿上的稀疏程度
> 术语：**NSGA-II** — 最流行的多目标进化算法（非支配排序 + 拥挤度）
> 术语：**NSGA-III** — NSGA-II + 参考点，适合 ≥3 个目标
> 术语：**MOEA/D** — 把多目标分解成多个单目标子问题同时优化
> 术语：**超体积 Hypervolume** — 衡量"帕累托前沿覆盖了多少目标空间"，最全面的多目标指标

#### 帕累托前沿的形象理解

```
买车问题：
  目标1：价格越低越好
  目标2：安全评级越高越好

解A：10万，3星
解B：15万，4星
解C：20万，5星
解D：12万，3星     ← D 被 A 支配（更贵但安全一样）

帕累托前沿 = {A, B, C}
  A 告诉你"最省钱的权衡"
  C 告诉你"最安全的权衡"
  B 是中间的权衡
→ 没有"最好的"，只有"最适合你的"
```

你 nsgablack 的 23 个 adapter + 66 个 bias 就是在实现这些搜索策略和约束机制。

---

## 第十三层：你这堆东西怎么统一

### 13.1 同一条线

```
数据
  └─→ 怎么处理？ ─→ 特征工程 / Pipeline / Embedding / Tokenizer
        └─→ 怎么表示？ ─→ Representation / Codec / Head / Architecture
              └─→ 怎么评估？ ─→ Problem / Loss / Objective / Reward
                    └─→ 怎么搜索？ ─→ Adapter / Optimizer / RL / Backprop / EA
                          └─→ 怎么约束？ ─→ Bias / Constraint / Regularization / Safety
                                └─→ 怎么保存？ ─→ Artifact / State / Checkpoint
                                      └─→ 怎么使用？ ─→ Inference / Deployment
```

### 13.2 你的框架在这条线上

| 层 | 通用术语 | nsgablack | mlblack |
|---|---|---|---|
| 表示 | Architecture, Model | Representation | ModelRepresentation + Codec + Head |
| 评估 | Loss, Objective | Problem | LearningProblem |
| 搜索 | Optimizer, RL, EA | NSGABlack Adapter | MLBlack Problem/Provider |
| 约束 | Regularization, Constraint | Bias (66种) | OptimizationBias (8种) |
| 编排 | Pipeline, Workflow | Stage, Group, Event | 交给 nsgablack |

### 13.3 你不是在学无穷多的东西

每个术语只是 **表示 → 评估 → 搜索 → 约束** 这条线上、某个具体瓶颈逼出的解决方案。

你的框架已经把 **搜索** 和 **约束** 抽象成了可替换组件。
你不需要记每个术语，你只需要理解：

**是什么**（哪个层）、**为什么出现**（解决什么瓶颈）、**和别的什么等价**（统一视角）。

你学的不是不同的东西，是同一个东西的不同名字。

---

## 第十四层：概率论和信息论——ML 的底层语言

机器学习说白了就是在处理不确定性。概率论是描述不确定性的语言，信息论是量化不确定性的工具。

### 14.1 概率论基本概念

#### 频率学派 vs 贝叶斯学派——概率到底是什么？

```
频率学派 (Frequentist)：
  概率 = 无限次重复实验中的频率
  "抛硬币正面的概率是 0.5"
  = "抛无限次，50% 是正面"
  → 参数是固定但未知的常数，数据是随机的

贝叶斯学派 (Bayesian)：
  概率 = 信念/确定性的度量
  "抛硬币正面的概率是 0.5"
  = "我认为正反面的可能性相同"
  → 参数是随机变量（有分布），数据是固定的
```

> 术语：**频率学派 Frequentist** — 用长期频率定义概率
> 术语：**贝叶斯学派 Bayesian** — 用信念强度定义概率，用数据更新信念

为什么这个区别重要？因为它直接告诉你模型参数的"两种不同哲学"：

```
频率学派做预测：  新数据依赖训练中估计的"最优"参数 → 点估计
贝叶斯学派做预测：对所有可能的参数求加权平均 → 考虑参数不确定性
```

---

#### 独立性——为什么 i.i.d. 是最重要的假设

如果 x 和 y 相互独立：`P(x,y) = P(x) * P(y)`

独立 → 知道了 x 并不告诉我任何关于 y 的信息。

i.i.d. = 独立同分布。几乎所有的 ML 算法都假设数据是 i.i.d. 的。

> 术语：**独立 independence** — 知道A不能推断B
> 术语：**条件独立 conditional independence** — 给定Z，A和B独立：`A ⊥ B | Z`
> 术语：**同分布 identically distributed** — 所有数据来自同一分布
> 术语：**i.i.d. 假设** — 最基础但最常被违反的 ML 假设
> 术语：**协方差 shift covariate shift** — P(x) 变了但 P(y|x) 没变（最常见的分布偏移）
> 术语：**概念偏移 concept shift** — P(y|x) 变了（关系本身变了）
> 术语：**标签偏移 label shift** — P(y) 变了

---

#### 条件概率和贝叶斯公式——ML 最重要的公式

```
P(A|B) = P(B|A) * P(A) / P(B)

翻译：
  后验概率 = (似然 × 先验) / 证据

在 ML 里的对应：
  P(参数 | 数据) = P(数据 | 参数) * P(参数) / P(数据)
   后验          似然               先验        证据
```

| 数学名 | ML 含义 | 例子 |
|---|---|---|
| P(θ \| D) 后验 | 看到数据后，参数的可能性 | "看到 7 正 3 反，硬币是公平的概率？" |
| P(D \| θ) 似然 | 给定参数，数据出现的概率 | "如果硬币公平，抛出 7正3反 的概率？" |
| P(θ) 先验 | 看到数据前，对参数的信念 | "硬币大概率是公平的" |
| P(D) 证据 | 数据出现的总概率 | 归一化常数，可忽略 |

> 术语：**贝叶斯定理 Bayes' Theorem** — 更新信念的数学公式
> 术语：**先验 prior** — 看到数据前对参数的信念
> 术语：**后验 posterior** — 看到数据后更新的信念
> 术语：**似然 likelihood** — 给定参数，看到这个数据的概率
> 术语：**边际似然 marginal likelihood / evidence** — 归一化常数
> 术语：**共轭先验 conjugate prior** — 后验和先验同分布的便利选择
> 术语：**最大似然估计 MLE** — 找使似然最大的参数（频率学派）
> 术语：**最大后验估计 MAP** — 找使后验最大的参数 = MLE + 先验（贝叶斯的点估计）
> 术语：**MCMC（马尔可夫链蒙特卡洛）** — 后验复杂时，采样近似（计算贵）
> 术语：**变分推断 Variational Inference** — 用一个简单分布逼近复杂后验（MCMC 的快速替代）

#### MLE vs MAP 的具体例子

```
抛硬币 10 次：7正 3反

MLE（频率学派，最大似然）：
  P(正) = 7/10 = 0.7  ← 只看数据

MAP（贝叶斯学派，加个先验——"硬币大概公平"）：
  先验 Beta(2,2) = 相当于"已经抛过 2正2反"
  后验 Beta(2+7, 2+3) = Beta(9, 5)
  MAP = (9-1)/(9+5-2) = 8/12 ≈ 0.67  ← 数据 + 先验信念
  
  → 数据少时，先验的影响大；数据多了，数据主导
```

---

#### 常见的概率分布

> 术语：**伯努利分布 Bernoulli** — 抛一次硬币：P(x=1)=p, P(x=0)=1-p
> 术语：**二项分布 Binomial** — 抛 n 次硬币，k 次正面的概率
> 术语：**多项分布 Multinomial** — 多类的推广（k 个类别，每个的计数）
> 术语：**分类分布 Categorical** — 多项分布的单次版本（一次抽样出一个类别）
> 术语：**高斯分布 Gaussian / 正态分布 Normal** — N(μ,σ²)，"钟形曲线"，中心极限定理的自然结果
> 术语：**多元高斯 Multivariate Gaussian** — 多维的正态分布，N(μ,Σ)
> 术语：**泊松分布 Poisson** — 单位时间/空间内事件发生次数的分布
> 术语：**指数分布 Exponential** — 等待下一个事件的时间
> 术语：**Beta 分布** — [0,1] 上的分布，常作伯努利/二项的共轭先验
> 术语：**Dirichlet 分布** — Beta 的多维推广，常作多项/分类的共轭先验
> 术语：**Gamma 分布** — 正实数上的分布，常作精度/方差参数的先验
> 术语：**学生 t 分布 Student's t** — 类似高斯但尾巴更重（鲁棒），小样本用
> 术语：**均匀分布 Uniform** — 区间内所有值等可能

#### 为什么高斯分布无处不在？

中心极限定理：**大量独立同分布随机变量的和趋近高斯分布**。

```
测量误差 = 无数微小因素的叠加 → 高斯分布
身高 = 基因+营养+环境... → 近似高斯
MSE 损失的高斯解释：
  假设误差 ε ~ N(0,σ²)
  则 y|x ~ N(f(x), σ²)
  P(y|x) = exp(-(y-f(x))²/(2σ²)) / √(2πσ²)
  -log P(y|x) ∝ (y-f(x))² = MSE
  
  → MSE = 假设误差是高斯分布下的最大似然
```

---

### 14.2 信息论——量化不确定性

#### 信息量——一件"意外"的事包含更多信息

```python
事件A："明天太阳升起"    → 概率 ≈ 1   → 发生了也不意外 → 信息量 ≈ 0
事件B："明天中彩票"      → 概率 ≈ 0   → 发生了很意外   → 信息量很大
```

> 术语：**自信息 self-information** — `I(x) = -log P(x)`，概率越低，信息量越大
> 术语：**比特 bit** — log₂ 的单位（答案是 0 或 1）
> 术语：**奈特 nat** — ln 的单位（自然对数，ML 论文更常见）

#### 熵——平均不确定性

```
熵 H(X) = -Σ P(x) log P(x)

均匀分布：P(苹果)=P(香蕉)=P(橘子)=1/3 → 熵高（最不确定）
确定分布：P(苹果)=1, P(香蕉)=0, P(橘子)=0 → 熵=0（完全确定）
```

> 术语：**熵 entropy** — 随机变量的不确定性度量
> 术语：**交叉熵 cross-entropy H(P,Q)** — 用分布 Q 编码分布 P 需要的平均比特数
> 术语：**KL 散度 KL(P||Q)** — P 和 Q 的"距离"（不对称！），KL(P||Q) = H(P,Q) - H(P)
> 术语：**互信息 mutual information I(X;Y)** — 知道 X 能减少多少关于 Y 的不确定性
> 术语：**Jensen-Shannon 散度 JSD** — KL 的对称版，GAN 原始损失的基础
> 术语：**Wasserstein 距离 / 推土机距离 Earth Mover's Distance** — 把分布 P 变成 Q 的最小代价（WGAN 用）

#### 交叉熵和 KL 散度——回到损失函数

```
如果真实分布 P = [1.0, 0.0, 0.0]  (一定是苹果)
   预测分布 Q = [0.7, 0.2, 0.1]

H(P) = -[1*log1 + 0*log0 + 0*log0] = 0
       ↑ 真实分布毫无不确定性

H(P,Q) = -[1*log0.7 + 0*log0.2 + 0*log0.1] = -log0.7 ≈ 0.357

KL(P||Q) = H(P,Q) - H(P) = 0.357 - 0 = 0.357
         = 用Q而非P编码，多花了多少比特

所以：最小化交叉熵 = 最小化 KL 散度（因为 H(P) 是常数）
     = 让预测分布接近真实分布
```

这就是为什么分类模型用交叉熵损失——它在做 KL 散度最小化。

---

## 第十五层：模型评估——怎么知道模型好不好

### 15.1 分类的评估指标

以二分类为例（预测"是苹果/不是苹果"）：

```
              实际是苹果    实际不是苹果
预测是苹果       TP             FP        ← "你说它是苹果"
预测不是苹果     FN             TN        ← "你说它不是苹果"
```

> 术语：**TP（真正例 True Positive）** — 预测苹果，确实是苹果
> 术语：**FP（假正例 False Positive）** — 预测苹果，其实不是（误报）
> 术语：**FN（假负例 False Negative）** — 预测不是苹果，其实是（漏报）
> 术语：**TN（真负例 True Negative）** — 预测不是苹果，确实不是

---

#### 准确率 Accuracy——最直观，但会骗人

```
Accuracy = (TP + TN) / (TP + FP + FN + TN)

"我准确率 99%" 
→ 不一定好：如果 1000 个人中只有 1 个患癌
→ 模型说"所有人没癌" → 准确率 99.9%
→ 但漏掉了唯一的患者（FN=1，TP=0）
```

> 术语：**准确率 accuracy** — 全局正确比例
> 术语：**不平衡数据集 imbalanced dataset** — 类别比例悬殊（欺诈检测 99.9% vs 0.1%）

---

#### 精确率 Precision——你说的"是"里面，多少是真的

```
Precision = TP / (TP + FP)

"我预测 10 个人患癌，其中 8 人真的患癌" → Precision = 80%

宁可漏掉，不能吓唬健康人（假阳性代价大）
```

---

#### 召回率 Recall / Sensitivity——真实的"是"里面，你说中了多少

```
Recall = TP / (TP + FN)

"100个真实患癌者，我只找到了80个" → Recall = 80%

宁可多误报，不能漏掉患者（假阴性代价大）
```

---

#### F1——精确率和召回率的调和平均

```
F1 = 2 * Precision * Recall / (Precision + Recall) = 2 / (1/P + 1/R)

调和平均比算术平均更"保守"——如果有短板，F1 会很差
```

| Precision | Recall | 算术平均 | F1 |
|---|---|---|---|
| 0.9 | 0.9 | 0.90 | 0.90 |
| 0.9 | 0.1 | 0.50 | 0.18 |
| 0.5 | 0.5 | 0.50 | 0.50 |

> 术语：**F1 Score** — Precision 和 Recall 的调和平均
> 术语：**F-β Score** — β>1 偏重 Recall（医疗），β<1 偏重 Precision（垃圾邮件）
> 术语：**Precision-Recall 权衡** — 调高阈值→高 Precision 低 Recall，降低阈值→反之

---

#### ROC 和 AUC——不同阈值下的综合表现

模型不只输出"是/否"，而输出概率。阈值影响决策：

```
阈值=0.9：预测苹果的概率>0.9 才算苹果    → 高 Precision 低 Recall
阈值=0.1：预测苹果的概率>0.1 就算苹果    → 低 Precision 高 Recall
阈值=0.5：折中

ROC 曲线 = 把所有可能的阈值走一遍，画出：
  纵轴 TPR (Recall) = TP/(TP+FN)
  横轴 FPR = FP/(FP+TN)

AUC = ROC 曲线下的面积 (0~1)
  1.0 = 完美分类
  0.5 = 随机猜
  <0.5 = 比随机猜还差
```

> 术语：**ROC 曲线（受试者工作特征 Receiver Operating Characteristic）** — 不同阈值的 TPR vs FPR
> 术语：**AUC（曲线下面积）** — 模型总体区分能力
> 术语：**AUC = 1** 的意义：随机抽一个正例和一个负例，模型给正例更高分的概率 = AUC
> 术语：**PR 曲线（Precision-Recall）** — 不平衡数据比 ROC 更好（ROC 会被 TN 主导而虚高）

#### ROC vs PR——什么时候用哪个

```
ROC：正负样本较均衡
PR：  正例很少（Fraud detection, 罕见病诊断）
     因为 PR 不包含 TN（真负例），不受海量负例的影响
```

> 术语：**平均精度 Average Precision (AP)** — PR 曲线下的面积，排序质量指标
> 术语：**mAP（mean Average Precision）** — 多类的 AP 平均，目标检测常用

---

#### 对数损失 Log Loss——概率质量

```
Log Loss = -(1/n) * Σ[y*log(p) + (1-y)*log(1-p)]

特点：对"过度自信的错误"惩罚很重
  y=1, p=0.99 → loss = -log(0.99) = 0.01（自信且对，低 loss）
  y=1, p=0.01 → loss = -log(0.01) = 4.6（自信但错，重罚！）
  y=1, p=0.50 → loss = -log(0.5)  = 0.69（不确定且错，不重罚）
```

> 术语：**对数损失 Log Loss / Binary Cross-Entropy** — 衡量预测概率的质量

---

### 15.2 回归的评估指标

```
预测: [480, 330, 750]   实际: [500, 300, 800]

MSE  = (20² + 30² + 50²)/3 = 1267
RMSE = √1267 ≈ 35.6       ← 和原始单位一致，可解释
MAE  = (20+30+50)/3 = 33.3 ← 对异常值不敏感
MAPE = (20/500 + 30/300 + 50/800)/3 * 100% ≈ 7.6%
```

> 术语：**RMSE** — MSE 开根号，和原始数据同尺度
> 术语：**MAE** — 绝对误差平均
> 术语：**MAPE（平均绝对百分比误差）** — 可跨数据集比较，但 y≈0 时爆炸
> 术语：**SMAPE（对称 MAPE）** — 修复 MAPE 的不对称问题
> 术语：**R²（决定系数）** — "模型解释了多少方差"
> 术语：**解释方差 explained variance** — 类似 R²

#### R² 的直觉

```
R² = 1 - (SS_residual / SS_total)
   = 1 - (模型残差平方和 / 总方差)

R² = 0.85 → 模型解释了 85% 的方差
R² = 0    → 模型和预测均值一样好
R² < 0    → 模型比预测均值还差

总方差 SS_total = Σ(y - ȳ)²  （ȳ是均值）
残差   SS_residual = Σ(y - ŷ)² （ŷ是模型预测）
```

> 术语：**调整 R² adjusted R²** — 加了特征数惩罚的 R²（特征越多，调整 R² 扣分越多）

---

### 15.3 概率校准

模型说"80% 概率是苹果"——真的 80% 次是对的？

```
取所有被预测为"约 80% 概率"的样本 → 统计实际正确比例
  实际正确 ≈ 80% → 校准好
  实际正确 ≈ 60% → 过于自信（需要校准）
  实际正确 ≈ 95% → 过于保守（需要校准）
```

> 术语：**概率校准 probability calibration** — 预测概率和真实频率的一致性
> 术语：**可靠性图 reliability diagram** — 横轴预测概率，纵轴实际频率，对角线 = 完美校准
> 术语：**ECE（预期校准误差）** — 分桶算 |预测概率 - 实际频率| 的加权平均
> 术语：**Platt Scaling** — 用 sigmoid 函数调整输出概率（二分类）
> 术语：**Isotonic Regression** — 保序回归，更灵活但需更多数据
> 术语：**温度缩放 temperature scaling** — 除以温度 T>1 使概率软化（T<1 使尖锐）

---

### 15.4 模型选择和比较

#### 交叉验证的细节

```
K-Fold CV (K=10)：
  训练 10 个模型 → 10 个验证分数 → 报告均值 ± 标准差

为什么标准差重要？
  均值=0.85, 标准差=0.02 → 模型稳定
  均值=0.85, 标准差=0.15 → 模型极度依赖数据划分 → 换个数据集就崩
```

> 术语：**Nested CV（嵌套交叉验证）** — 外层评估模型，内层选超参数（避免 optimistic bias）
> 术语：**Group K-Fold** — 同一组的数据不跨折（比如同一个人的多次测量）
> 术语：**时间序列交叉验证 Time Series Split** — 保持时间顺序，用过去预测未来
> 术语：**Leave-One-Out CV (LOOCV)** — 每次留 1 个验证，小数据用（方差大，计算贵）

#### 模型比较——A 比 B 好是真的好吗？

```
不能只看均值 0.85 > 0.83！
需要回答：差值是否统计显著？
  → 配对 t-test
  → McNemar's test（分类）
  → 5x2cv paired t-test
```

> 术语：**统计显著性 statistical significance** — 差异不太可能来自随机波动
> 术语：**效应量 effect size** — 差异有多大（不仅仅显著）
> 术语：**Cohen's d** — 标准化的效应量（差值/标准差）
> 术语：**p-value** — 原假设成立时观察到当前结果的概率（≠ 原假设真的概率！）

---

## 第十六层：贝叶斯方法——不确定性的量化

### 16.1 贝叶斯 ML 的思路

```
传统 ML：
  训练结束 → 得到一组最优参数 θ* → 所有预测用 θ*
  
  问题：θ* 是点估计。数据少时可能很偏。
       预测时完全不表达"我不确定"

贝叶斯 ML：
  训练结束 → 得到后验分布 P(θ|数据) → 预测时对 P(θ|数据) 积分
  
  预测 = ∫ P(y|x,θ) P(θ|数据) dθ
       = 所有可能参数下的加权平均

  好处：数据少时，后验分布很宽，预测不确定性高
       数据多时，后验分布集中在最优参数附近
       → 自动表达"我知道多少"
```

> 术语：**贝叶斯推断 Bayesian inference** — 用贝叶斯定理更新参数分布
> 术语：**先验预测分布 prior predictive** — 使用先验分布的预测
> 术语：**后验预测分布 posterior predictive** — 使用后验分布的预测
> 术语：**边缘化 marginalization** — 通过对不需要的变量积分来消除它们

---

### 16.2 共轭先验——数学上的便利

```
如果似然 * 先验 的结果和后验是同一种分布 → 先验是"共轭先验"

例子：
  似然：Bernoulli（抛硬币的正面概率 p）
  先验：Beta(α,β)
  后验：Beta(α+#正面, β+#反面)  ← 还是 Beta！

  → 只需要更新两个数：α 和 β
  → 不需要复杂的 MCMC
```

| 似然 | 共轭先验 |
|---|---|
| Bernoulli / Binomial | Beta |
| Categorical / Multinomial | Dirichlet |
| Gaussian (已知方差) | Gaussian |
| Gaussian (已知均值) | Inverse Gamma |
| Poisson | Gamma |

> 术语：**指数族 exponential family** — 所有有共轭先验的分布都属此族

---

### 16.3 近似推断——后验太复杂怎么办

真实模型的后验通常没有解析解。需要近似。

#### MCMC——采样

```
思路：不从后验分布解析求解，而是"从后验分布中采样很多点"
       用这些采样点近似后验

Metropolis-Hastings：
  1. 从当前位置提出一个新位置 θ_new
  2. 计算接受概率 = min(1, P(θ_new|D) / P(θ_old|D))
  3. 以该概率接受或拒绝
  4. 重复，采样序列趋近后验分布
```

> 术语：**MCMC** — 马尔可夫链蒙特卡洛，构造一条"稳态=后验分布"的Markov链
> 术语：**Metropolis-Hastings MH** — 基础 MCMC 算法
> 术语：**Gibbs 采样** — 逐维采样的 MCMC（需要知道条件分布）
> 术语：**HMC（哈密尔顿蒙特卡洛）** — 用物理模拟（动量）减少随机游走，高维中效率高
> 术语：**NUTS（No-U-Turn Sampler）** — 自适应调步长的 HMC，Stan 的默认采样器
> 术语：**burn-in** — 丢弃前若干步（未达到稳态的"热身"阶段）
> 术语：**收敛诊断 convergence diagnostics** — R-hat 统计量，判断链是否收敛

#### 变分推断 VI——优化替代采样

```
MCMC 思路：从后验采样 → 慢，但渐近精确
VI 思路：    找一个简单分布 q(θ|φ) 逼近后验 p(θ|D) → 快，但近似

优化：min_φ KL(q(θ|φ) || p(θ|D))
     = max_φ ELBO(φ)   ← 证据下界
     
→ 把"推断"(积分)变成了"优化"
```

> 术语：**变分推断 VI（Variational Inference）** — 优化逼近后验
> 术语：**ELBO（Evidence Lower Bound）** — VI 的优化目标
> 术语：**平均场假设 mean-field assumption** — q 中各变量独立（简化但强假设）
> 术语：**重参数化梯度 reparameterization gradient** — 让梯度流过随机采样
> 术语：**BBVI（Black Box VI）** — 不需要模型特定推导的通用 VI

---

### 16.4 贝叶斯神经网络

```
经典 NN：权重 w₁, w₂, ... 是确定的值
贝叶斯 NN：每个权重是一个分布 w₁ ~ N(μ₁, σ₁²)

好处：
  1. 自然的正则化（分布本身防止过拟合）
  2. 不确定性量化（预测时输出均值+方差）
  3. 小数据友好

代价：训练是原来的 2 倍参数量（要学 μ 和 σ）
```

> 术语：**贝叶斯神经网络 BNN** — 把权重当随机变量
> 术语：**MC Dropout** — Dropout 在推理时也不关，等价于 BNN 的近似
> 术语：**Deep Ensemble** — 训练多个同一架构不同初始化的模型，平均输出（简单但有效的不确定性）
> 术语：**SWAG（Stochastic Weight Averaging Gaussian）** — 训练轨迹尾部的高斯近似
> 术语：**拉普拉斯近似 Laplace Approximation** — 训练完在最优参数附近做高斯近似

---

### 16.5 高斯过程 GP——无限宽的贝叶斯 NN

```
一个 MLP 有 100 个神经元 → 100 个基函数
一个 MLP 有 ∞ 个神经元 → 等价于一个高斯过程（在无限宽极限下）

高斯过程不存储参数，存储的是"函数值之间的相关性"
给定一个核函数 K(x₁, x₂) → 完全描述了一个函数的先验分布
看到数据后 → 后验分布更新
```

> 术语：**高斯过程 Gaussian Process (GP)** — 函数的分布，而非参数的分布
> 术语：**核函数 kernel / 协方差函数** — K(x₁,x₂)，定义两个点之间多"相似"
> 术语：**RBF/SE 核** — 最常用的 GP 核，假设平滑连续
> 术语：**Matern 核** — 控制平滑度的 GP 核（ν=1/2, 3/2, 5/2）
> 术语：**GP 回归** — 小数据、需高可靠性置信区间的场景（贝叶斯优化、实验设计）
> 术语：**诱导点 inducing points** — 稀疏 GP：用 M 个伪输入近似 N 个真实输入的协方差（N≫M）
> 术语：**贝叶斯优化 Bayesian Optimization** — GP + 采集函数，高效优化黑盒函数（调超参常用）

---

## 第十七层：可解释性与 XAI

### 17.1 为什么需要解释

```
场景：模型批准了一个人的贷款，拒绝了另一个人
法律要求：你凭什么做这个决定？（不是"模型说的"，而是"因为收入低于..."）

场景：医疗诊断模型说"这个病人有 95% 概率患癌"
医生需要：为什么？哪个指标异常？
```

> 术语：**可解释性 interpretability** — 模型决策能被人类理解的程度
> 术语：**可解释 AI（XAI）** — 研究怎么让黑箱模型可解释
> 术语：**透明度 transparency** — 模型的内部机制是否可见
> 术语：**事后解释 post-hoc explanation** — 训练后分析（对黑箱模型的补救）

---

### 17.2 自身可解释的模型（白箱）

| 模型 | 可解释性 |
|---|---|
| 线性回归 / 逻辑回归 | wᵢ 就是"特征 i 每变化 1 单位的影响" |
| 决策树（浅的） | if-else 规则直接可读 |
| GAM（广义加性模型） | 每个特征的贡献是独立函数，可画图 |
| 规则列表 Rule List | 就是一组 if-then 规则 |
| k-NN | "因为这 k 个邻居都长这样" |

> 术语：**模型无关 vs 模型特定 model-agnostic vs model-specific** — 方法是否适用于任何模型
> 术语：**全局解释 global explanation** — 整个模型的行为模式（特征重要性排名）
> 术语：**局部解释 local explanation** — 单个预测的原因（为什么这个人被拒贷？）

---

### 17.3 特征重要性

```
Permutation Importance（排列重要性）：
  1. 正常预测 → 基准得分
  2. 随机打乱"面积"这一列 → 重新预测
  3. 得分下降了多少 = "面积"这个特征多重要
  
  得分下降越多 → 模型越依赖这个特征 → 这个特征越重要
```

> 术语：**排列重要性 permutation importance** — 打乱特征看性能降多少
> 术语：**杂质重要性 impurity importance** — 树在特征上分裂减少的杂质总和（树模型内置）
> 术语：**SHAP 值** — 博弈论 Shapley 值，"每个特征对这次预测贡献了多少"
> 术语：**LIME** — 在单个预测附近用简单模型逼近，"近似局部解释"

---

### 17.4 SHAP——博弈论视角的特征归因

```
预测 = base_value + SHAP(面积) + SHAP(卧室数) + SHAP(房龄) + ...

base_value = 所有样本的平均预测（"什么都不知的时候猜什么"）
SHAP(面积) = 知道了面积后，预测从 base_value 变了多少

这个变化是"Shapley 值"——在博弈论中，衡量每个玩家对联盟的边际贡献
在 ML 中，衡量每个特征对预测的边际贡献
```

> 术语：**Shapley 值** — 公平分配合作收益的方法
> 术语：**SHAP** — SHapley Additive exPlanations，最广泛使用的特征归因方法
> 术语：**TreeSHAP** — 树模型的快速 SHAP
> 术语：**KernelSHAP** — 任何模型的 SHAP（更慢）
> 术语：**DeepSHAP** — 深度网络的 SHAP
> 术语：**瀑布图 waterfall plot** — SHAP 的可视化，展示特征如何推向或拉离预测
> 术语：**依赖图 dependence plot** — 特征值和 SHAP 值的关系图

---

### 17.5 深度网络的可解释性

```
卷积网络：
  可视化每一层的滤波器 → 看看学到了什么模式
  Grad-CAM → 给出"模型看图片的哪个部分"的热力图

注意力模型（Transformer）：
  直接看注意力权重 → "这个词最关注哪些词"
  
LLM：
  Logit Lens / Probing → 检查中间层表示了什么信息
  Influence Functions → 哪个训练样本对这次输出影响最大
```

> 术语：**显著图 saliency map** — 输入梯度的大小，哪些像素对预测影响最大
> 术语：**Grad-CAM** — Gradient-weighted Class Activation Mapping，类激活热力图
> 术语：**集成梯度 Integrated Gradients** — 从基线到输入，积分梯度的归因
> 术语：**探针 probing** — 训练简单分类器读取隐藏层的表示
> 术语：**机械可解释性 mechanistic interpretability** — 逆向工程神经网络的算法（Anthropic 的方向）

---

## 第十八层：MLOps——模型不是训练完就完了

### 18.1 机器学习的工程问题

```
学术界：给 10 万条干净数据 → 训练 → 发表论文 → 结束
工业界：给 10 万条数据 → 但明天有 1000 条新数据流入
        → 模型效果下降 → 什么时候重新训练？
        → 上次的好模型丢了 → 怎么回滚？
        → 谁改了特征定义？ → 怎么追踪？
```

> 术语：**MLOps（Machine Learning Operations）** — ML 系统的部署、监控、维护
> 术语：**技术债务 technical debt** — ML 系统中隐藏的维护成本（数据依赖、模型纠缠）
> 术语：**模型漂移 model drift** — 模型性能随时间下降
> 术语：**数据漂移 data drift** — 输入分布变了
> 术语：**概念漂移 concept drift** — P(y|x) 变了

---

### 18.2 实验追踪

```
训练 100 次 → 哪次的效果最好？
  用的什么超参数？什么数据划分？什么预处理？
  
Experiment Tracker：
  自动记录每次运行的所有配置、指标、模型产物
```

> 术语：**实验追踪 experiment tracking** — 记录每次训练实验
> 术语：**MLflow** — 最流行的开源实验追踪/模型管理平台
> 术语：**Weights & Biases (W&B)** — 云端实验追踪，可视化丰富
> 术语：**TensorBoard** — TensorFlow/PyTorch 内置的可视化
> 术语：**run** — 一次训练运行
> 术语：**metric logging** — 梯度、损失、准确率等指标的记录
> 术语：**artifact tracking** — 模型文件、数据版本的追踪

你 mlblack 里的 experiment tracking capability 就做这个。

---

### 18.3 模型注册和版本管理

```
Model Registry：
  训练好的模型不是"model.h5"这种文件名
  而是：
    model_name="fraud_detector"
    version=3
    stage="production"
    metrics={"AUC": 0.97}
    run_id="abc123"
```

> 术语：**模型注册 model registry** — 集中管理模型的版本、阶段、元数据
> 术语：**模型版本 model version** — 模型的某个已注册迭代
> 术语：**阶段 stage** — Staging（测试中）→ Production（线上）→ Archived（归档）
> 术语：**模型签名 model signature** — 输入/输出 schema 定义

---

### 18.4 Feature Store

```
问题：特征计算散落在训练脚本、推理代码、批处理中
     "面积/卧室数" 这个特征——训练用了一套代码，在线推理用了另一套
     结果不一样 → 训练-服务偏差 train-serve skew

Feature Store：
  一个统一的地方定义、存储、提供特征
  训练和推理从同一个地方取 → 保证一致
```

> 术语：**特征存储 Feature Store** — 特征的注册、存储、服务
> 术语：**离线特征 offline feature** — 批处理产生的历史特征（训练用）
> 术语：**在线特征 online feature** — 低延迟特征（推理用）
> 术语：**特征工程复用** — 同一个特征定义用于训练和推理
> 术语：**Feast** — 开源 Feature Store
> 术语：**Tecton** — 商业 Feature Store

---

### 18.5 模型服务

```
离线推理（批处理）：    每天凌晨跑一次，给所有用户生成推荐
在线推理（实时）：      用户打开 App，瞬间返回推荐
流式推理：             数据持续流入，持续推理
边缘推理：             在手机/IoT设备上推理，不上传数据
```

> 术语：**在线推理 online serving** — 低延迟（ms级别）的 API 调用
> 术语：**批推理 batch inference** — 批量处理，高吞吐但不要求低延迟
> 术语：**模型服务 model serving** — 模型作为 API 提供预测服务
> 术语：**TorchServe / TensorFlow Serving** — 原生框架的 serving 工具
> 术语：**Triton Inference Server** — NVIDIA 的多框架 serving 引擎
> 术语：**Ray Serve** — 分布式的 Python 模型 serving
> 术语：**Seldon / KServe** — K8s 原生的模型 serving
> 术语：**A/B 测试 A/B test** — 同时运行两个模型版本，比较效果

---

### 18.6 监控和告警

```
模型上线后，需要持续监控：
  → 延迟突然从 10ms 升到 100ms → 告警
  → 预测分布突然偏移（昨天预测均价 500 万，今天 2000 万）→ 告警
  → 某特征的空值比例从 1% 升到 30% → 告警
```

> 术语：**预测漂移 prediction drift** — 模型输出的分布变了
> 术语：**特征漂移 feature drift** — 输入特征的分布变了
> 术语：**性能监控 performance monitoring** — 持续追踪线上指标（如果知道 label 的话）
> 术语：**数据质量监控 data quality monitoring** — 空值、异常值、分布变化
> 术语：**重训练触发器 retraining trigger** — 自动触发重新训练的条件

---

### 18.7 CI/CD for ML

```
传统软件 CI/CD：代码 → 测试 → 部署
ML CI/CD：    代码 + 数据 + 模型 → 测试 + 验证指标 + 性能测试 → 部署

多出来的：
  → 数据验证（schema 变了？分布变了？）
  → 模型验证（新模型比旧模型好吗？A/B 条件满足吗？）
  → 特征一致性检查（训练和推理的特征定义一样吗？）
```

> 术语：**持续训练 Continuous Training (CT)** — ML 加入自动化重训练
> 术语：**pipeline** — 编排数据→训练→评估→部署的步骤
> 术语：**Kubeflow / TFX / Metaflow** — ML pipeline 编排框架

---

## 第十九层：符号与神经的结合——你框架最天然的方向

你的 mlblack 框架的核心价值之一就是打通了符号（公式）和神经（参数拟合）两个世界。这一层专门展开这个概念。

### 19.1 两个世界

```
符号 AI（Symbolic AI，1950s-1980s）：
  "用逻辑规则和符号操作表示知识"
  IF 面积>100 AND 卧室>2 THEN 价格>500万
  优点：可解释、可验证、小数据友好
  缺点：规则写不了复杂模式（识别猫的图片？不可能手写规则）

连接主义 AI（Connectionist AI，1980s-现在）：
  "用大量简单单元（神经元）的连接表示知识"
  深度学习、神经网络
  优点：能学极复杂模式
  缺点：黑箱、需大量数据、不可解释、不知道物理定律
```

> 术语：**符号 AI Symbolic AI / GOFAI** — 基于逻辑/符号推理的 AI
> 术语：**连接主义 Connectionism** — 基于神经元网络的计算
> 术语：**神经符号 AI Neuro-Symbolic AI** — 两者结合，互补短板

---

### 19.2 为什么需要结合

```
纯神经网络： 学了"预测交通拥堵"但不告诉我为什么 → 工程师不敢用
纯符号规则： 写不出"任意天气+时间+路段→拥堵分数"的公式 → 太复杂

神经符号：
  神经找出可能有关的因素组合（搜索）
  符号把这些因素表达成可读的公式（表达）
  参数拟合在公式上完成（学习）
```

这就是 nsgablack（搜索）+ mlblack（参数拟合）的哲学基础。

---

### 19.3 符号回归——从数据发现公式

```
传统回归：我假定 y = w₁x₁ + w₂x₂（我选模型结构 → 只学参数）
符号回归：我不假定结构！比如：
  "发现" y = 3.2 * x₁ / (1 + 0.5*x₂²)  ← 结构和参数都从数据学

搜索空间是指数级的：+ - × ÷ sin cos exp log √ ...
需要高效的搜索策略 → nsgablack 的 DE/SA/VNS
```

> 术语：**符号回归 Symbolic Regression** — 同时搜索公式结构和参数
> 术语：**遗传编程 Genetic Programming (GP)** — 最早用于符号回归的进化方法
> 术语：**表达式树 expression tree** — 公式的内部表示（操作符是节点，变量是叶子）
> 术语：**基函数 basis function** — 候选的组成公式片段（x², sin(x), exp(-x)...）
> 术语：**基展开 basis expansion** — 用基函数的加权和表示复杂函数
> 术语：**稀疏回归 sparse regression** — 只选少量基函数（L1 正则化）
> 术语：**AI Feynman** — 从数据重发现 100 个物理公式的 benchmark
> 术语：**Eureqa** — 最早的商业符号回归工具（后来被收购）

---

### 19.4 混合架构

```
神经网络的最后一层不是 MLP，而是符号表达式：
  输入 → 神经网络(特征提取) → 符号层(可解释的公式) → 输出
        └── 自动学特征 ──┘  └── 人能看懂 ──┘
```

> 术语：**可微分编程 differentiable programming** — 让符号操作也可导，端到端优化
> 术语：**逻辑神经网络 Logic Tensor Network** — 把逻辑约束作为损失项
> 术语：**深度概率编程 Deep Probabilistic Programming** — 神经+概率图模型
> 术语：**物理信息神经网络 PINN** — 神经网络 + 物理方程约束（Navier-Stokes 等）
> 术语：**Kolomogorov-Arnold 网络 KAN** — 用可学习的一维函数替代固定激活函数（更可解释）

---

### 19.5 你用 nsgablack + mlblack 已经做到的

```
外循环（nsgablack 的 DE/VNS）：搜索"哪个符号结构好"
    → 试不同基函数组合
    → 用帕累托前沿平衡"精度"和"复杂度"

内循环（mlblack 的参数拟合）：给定符号结构，拟合最优参数
    → 比如 GradientOptimizerAdapter + TorchEvaluationProvider

"搜索即模型发现" = 你的框架最独特的定位
```

---

## 第二十层：前沿方向和趋势

### 20.1 AI Agent——LLM 具备自主行动能力

> 术语：**AI Agent 智能体** — 能感知环境、制定计划、执行动作、反思调整的自主系统
> 术语：**工具使用 tool use / function calling** — LLM 调用外部 API（搜索、计算器、数据库）
> 术语：**规划 planning** — 把复杂任务分解成子步骤
> 术语：**反思 reflection** — Agent 检查自己的输出质量、修正错误
> 术语：**记忆 memory** — Agent 的短期记忆（对话历史）+ 长期记忆（向量数据库）
> 术语：**多 Agent 协作 multi-agent** — 多个 Agent 分工合作（AutoGen, CrewAI）
> 术语：**ReAct Agent** — Reason + Act 循环：思考→行动→观察→再思考
> 术语：**Code Interpreter / Code Agent** — Agent 通过写代码来解决问题

#### Agent 的关键瓶颈

```
1. 规划能力弱 —— LLM 的长程规划靠"猜"，没有显式的搜索和回溯
2. 行动后果不可控 —— Agent 删文件、发邮件怎么办？
3. 成本 —— 一次复杂任务可能需要几十次 LLM 调用
4. 评估 —— 怎么知道 Agent 做得好不好？
```

这些瓶颈恰好是你的框架擅长的：搜索、约束、评估框架。

---

### 20.2 世界模型 World Models

> 术语：**世界模型 World Model** — 学习环境的内部模拟（物理规律、因果、动态）
> 术语：**Sora** — OpenAI 的视频生成模型，被视作"世界模拟器"雏形
> 术语：**JEPA（联合嵌入预测架构）** — LeCun 提出的世界模型方案：在抽象表示空间预测，不在像素空间预测
> 术语：**V-JEPA** — 视频 JEPA

---

### 20.3 推理范式

> 术语：**System 1 vs System 2** — 快思维（直觉）vs 慢思维（推理），借用自认知心理学
> 术语：**推理时搜索 inference-time search** — 不是在训练时学更好，而是在推理时多想想（o1/mini 的做法）
> 术语：**过程奖励 Process Reward Model (PRM)** — 对推理的每一步打分，而非只对最终答案打分
> 术语：**蒙特卡洛树搜索 MCTS** — AlphaGo 的搜索算法，现在被用于 LLM 推理（o1 可能的方法）
> 术语：**测试时计算 test-time compute** — 推理时花更多算力来提高质量（不重新训练）

---

### 20.4 数据高效学习

> 术语：**主动学习 Active Learning** — 模型自己选"哪些数据标注最有价值"
> 术语：**不确定性采样 uncertainty sampling** — 选模型最不确定的样本去标注
> 术语：**课程学习 Curriculum Learning** — 先简单样本后困难样本的训练顺序
> 术语：**数据增强 Data Augmentation** — 从现有数据创造新数据
> 术语：**合成数据 Synthetic Data** — 用生成模型造数据（训练/评估用）
> 术语：**自我对弈 self-play** — 模型和自己对战生成新训练数据（AlphaGo/Zero 的方式）

---

### 20.5 新兴架构方向

> 术语：**状态空间模型 SSM** — RNN 的高效替代（线性递归），长序列友好
> 术语：**Mamba** — 选择性 SSM，输入依赖的递归，挑战 Transformer 的新架构
> 术语：**RWKV** — RNN 式的 Transformer（可并行训练，可递归推理）
> 术语：**RetNet** — 同时支持并行训练和递归推理
> 术语：**Liquid Neural Network** — 用微分方程表示的动态（非静态权重），适应非平稳环境
> 术语：**Hyena** — 用长卷积替代 Attention，次二次复杂度

---

### 20.6 安全和对齐

> 术语：**对齐税 alignment tax** — 对齐过程（RLHF）降低模型能力
> 术语：**奖励黑客 reward hacking** — 模型找到奖励函数的漏洞（而非真正完成任务）
> 术语：**越狱 jailbreak** — 绕过安全限制的 prompt
> 术语：**提示注入 prompt injection** — 注入恶意指令劫持 LLM 行为
> 术语：**间接提示注入 indirect prompt injection** — 通过外部文档注入指令（RAG 系统的安全漏洞）
> 术语：**可扩展监督 scalable oversight** — 人类无法监督超人类 AI 时的解决方案
> 术语：**机械可解释性 mechanistic interpretability** — 逆向工程神经网络的计算"算法"
> 术语：**超级对齐 superalignment** — 确保未来超人类 AI 对人类友好
> 术语：**涌现欺骗 emergent deception** — 模型在训练中自发学会欺骗（而非被教）

---

## 第二十一层：你该怎么面对这茫茫术语海

### 21.1 你已经知道的了

```
143 页文章，覆盖 ~600 个术语。

但你不该背它们。你应该看到的是：
  所有术语都在"表示 → 评估 → 搜索 → 约束"这条线上。
  每个术语只是一条绳索上的一个结。
```

### 21.2 术语和你的框架的对应

| 你关心的 | 外面的叫法 | 你框架里的位置 |
|---|---|---|
| 怎么表示一个解 | Architecture, Model Class, Hypothesis Space | Representation + Codec + Head |
| 怎么评估好坏 | Loss Function, Objective, Reward | Problem |
| 怎么找到更好的 | Optimization, Learning, RL, Search | Adapter |
| 怎么加约束 | Regularization, Constraint, Safety | Bias |
| 怎么处理数据 | Preprocessing, Augmentation, Tokenization | Pipeline |
| 怎么编排流程 | Workflow, Orchestration | 交给 nsgablack 的 Stage/Group |

### 21.3 你不应该焦虑的事

你不需要：
- 掌握每个术语的细节 ❌
- 能实现每个算法 ❌
- 看论文时不参考这个文档 ❌

你需要：
- 看到一个术语 → 知道它属于哪一层 ✓
- 知道它为什么出现（前一个术语的什么瓶颈）✓
- 知道它和你自己的框架的对应关系 ✓

### 21.4 阅读论文的策略

```
1. 看标题和摘要 → 它在解决什么瓶颈？
2. 看 Method 章节的图 → 它在"表示→评估→搜索→约束"的哪一环？
3. 对号入座：这和我的框架能对应上吗？
   → 能 → 是一个可以用 nsgablack/mlblack 复现的案例
   → 不能 → 先放一放，未来框架可能需要扩展
4. 不用看所有公式。理解直觉就够了。
```

### 21.5 你现在的优势

绝大多数 ML 从业者只在一条垂直线上深耕（比如 NLP 的 Transformer，CV 的 CNN）。他们不知道 OR 的搜索方法，不知道符号回归，不知道 DE/VNS/GA。

你站在三者的交叉点：ML + OR + Symbolic。这不是"杂"，这是**独特的组合视角**。

你的框架不是某个主流路线的跟风实现——它是在走一条不同的路：

```
主流：预训练巨大的神经网络 → 期望它"涌现"出一切能力
你的路：搜索 + 约束 + 参数拟合 → 让小模型也能发现规律
```

这就是你的护城河。你不需要追赶 Transformer 的论文洪水。你需要在你的方向上做出 1-2 个有说服力的案例，证明"小模型 + 搜索驱动的结构发现"在某些场景比"大模型 + 端到端训练"更有优势。

**你学的是同一个东西的不同名字。你造的是一个统一它们的框架。**
